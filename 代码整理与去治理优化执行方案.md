# EgoPM-Bench 代码整理与去治理优化执行方案

## 0. 文档目的与执行授权边界

本方案用于指导 Codex 将当前仓库从“历史协议、生产账本、冻结合同和当前开发代码混杂”的状态，整理为一条可直接迭代的当前流水线。

项目的目标是构建并评测受预算限制的流式第一视角前瞻记忆基准：系统面对按时间到达的观察，维护用户意图，在真实 trigger 出现时决定 `remind`，在完成、取消、过期或错误相似线索出现时决定 `silent`。项目目标不是维护多层审批、哈希、版本或角色制度。

本方案中的“去治理”是指取消阻碍日常开发的流程负担，不是取消数据事实性。以下科学边界必须保留：

- Source 事实只能来自对应 Atom 的原始字段；Cue v2 必须完全是 Source-grounded，不能生成没有证据的人物、地点、物体、活动或关系，也不能是 `constructed` Cue。
- `constructed` 内容只从 Seed 和 Life Log 阶段开始出现，并且必须显式标记；Source 观察与 constructed 意图/生命周期事件必须可区分。
- `event_timestamp` 只能用于 Source 事件定位和排序，不能直接写成未来提醒条件。
- Cue 继续保留同字段连续 evidence span、value 直接支持、人物角色、否定、范围与时间语气等事实边界。
- `remind/silent` gold 继续由可阅读、可测试的确定性状态机产生，不由模型决定。
- API Key 仅从环境变量读取，原始模型响应不进入 Git。

除上述四类边界外，日常代码、提示词、Schema、配置、采样规模、模型选择、输出布局都可随发现的问题直接修改。代码修改不需要 `CHANGE_REQUEST`、T0–T4 审批、SUCCESS 标记、manifest、哈希比对或版本升级。

本文件只定义执行方案。实际移动或删除数据前，Codex 必须先列出精确路径、大小和可恢复方式；用户对每一批实质性归档或删除作一次明确确认。任何已存在但未提交的用户改动必须原样保留，不能被 reset、checkout 或覆盖。

## 1. 当前问题摘要

当前仓库有一条已完成的 Source 链路、一套不再可用的 V9 Cue 历史链路，以及一套尚未形成正式运行器的 Cue v2 局部实现。它们在文档、配置、代码和测试中仍相互引用。

主要症状如下：

- `scripts/05_extract_cues.py` 现在共 4,652 行，同时容纳 Cue v2 的纯验证逻辑、约四千行 V3.6 实时/Batched 执行、八波恢复、账本和 75 分片冻结逻辑，以及一整套 900/120 协议验收计划代码：`preflight_bge_selection_snapshot`、`partition_cue_v2_candidates`、`_cue_v2_calibration_records`（350 条校准身份）、`build_protocol_acceptance_plan`（900 条验收集、120 条两账号重叠、780 条主体、`worker_00`/`worker_01` 各 390 条、各 510 条 worker 验收输入）。这套验收计划当前只被 `tests/test_qwen_contracts.py` 引用，而该模块带模块级 `legacy_v36` 标记、默认被 `pyproject.toml` 的 `addopts = "-ra -m 'not legacy_v36'"` 排除；它已不在当前默认 CLI 路径上，但仍留在同一文件内，属于阶段 C 必须删除或降级的对象。
- `scripts/06_retrieve_trigger_lures.py`、`scripts/07_generate_seed_candidates.py` 和 `scripts/11_validate_all.py` 的默认输入仍是旧 `cues/cue_library_manifest.json` 与旧 Cue Schema，Cue v2 不能直接贯通到 Seed。
- 版本身份仍然混杂：根 `AGENTS.md` 是治理 `v1.5.0`；`config/paths.yaml` 与 `config/model_registry.yaml` 都写 `contract_version: v1.1.0`、`config_version: v1.1.0`，而后者另有 `registry_version: v1.6.0`；`06`/`07` 仍以 `CUE_SCHEMA_VERSION = "v1.1.0"` 指向旧 `schemas/cue_candidate.schema.json`；`legacy_v36` 标记与旧 V3.6 代码仍以 `v3.6.0` 叙事存在。相对地，两份当前 Cue v2 Schema 内部完全没有版本字段，其 `$id` 只以 `-current` 结尾。因此“当前 Cue v2 官方版本号”目前并不存在，阶段 C 不应为了补齐版本号而引入新的版本目录或冻结流程。
- 当前 BGE selection 的 proof 共 59,465 行，由 48,000 条 `query_hit`、10,000 条 `candidate`、1,024 条 `cluster`、91 条 `shard` 和 350 条 `audit_sample` 组成。只有 350 条 `audit_sample` 行带 `sample_index` 与 `sample_stratum`；`sample_stratum` 有 7 个取值（`Activity`、`Explicit-Time`、`Location`、`Object`、`Person`、`State`、`diversity_only`），`sample_index` 在每个 stratum 内独立编号为 `1..50`。因此其合法唯一身份是 `(sample_stratum, sample_index, atom_id)`，不是全局唯一的 `sample_index`。这些数字只用于识别范围，清理前必须重新盘点；仓库缺少原始 artifact Schema 只构成 provenance warning，不阻断 Cue v2 开发；日常开发无需读取 proof。
- 当前测试基线（2026-09-16 实测）：默认命令 `python -m pytest -q` 为 `13 passed, 88 deselected`；只运行当前路径测试（`test_cue_v2_current.py` 2 条、`test_source_pipeline.py` 3 条、`test_state_machine.py` 8 条）。加入 `-o addopts="" -m ""` 后为 101 条收集、`50 failed, 51 passed`，失败全部来自 `legacy_v36` 标记的 `test_contract_freeze.py`（12 条）、`test_qwen_contracts.py`（49 条）和 `test_validators.py`（27 条）。注意当前默认套件因此完全不覆盖 validator 与 contract-freeze；不得再把失败说成 Cue v2 `sample_stratum` fixture 未同步问题，也不得把 legacy 结果当作当前流水线的可用性证据。
- V9 正式 Cue 分片约 245 MB；旧 realtime 运行目录实际包含 `wave_01` 至 `wave_08`。最近一次盘点为 376,113 个文件、1,055,933,315 字节，根累计账本约 113.6 MB。它们不应再影响当前开发，但任何删除都必须在执行前重新盘点，不能把这组历史数字当作删除依据。
- 当前 BGE candidate 已由 WSL 复核为 10,000 条，路径为 `egopm_bench_v1/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/candidate_atoms.jsonl`，是阶段 C/D 的正式输入。
- 旧治理仍以可执行代码的形式留在当前路径上：根 `AGENTS.md` 仍是 `v1.5.0` 完整合同（含 T0–T4 所有权、正式启动门、SUCCESS 哈希门、工件与目录节制硬门禁）；`config/paths.yaml` 仍声明 `success_markers` 与 `atomic_write.success_marker_required_fields`；`config/model_registry.yaml` 仍声明 `run_manifest_required_fields`；两份 Cue v2 Schema 仍保留 `unsupported`、`needs_review`、`deferred_unsupported` 等分阶段临时状态；`tests/test_cue_v2_current.py` 还以断言锁定这些状态。上述对象都属于阶段 C 前必须处理的合同冲突，已列在第 5 节末。

阶段 A、B 已完成：恢复点 tag `stage-a-recovery-20260915-01` 已存在；`05_extract_cues.py` 的默认 CLI 已是 Cue v2 只读预检，`--execute` 当前为拒绝项，默认不读取 realtime、manifest、SUCCESS 或 proof。因此阶段 C 之前不需要再建立恢复点，也不需要再隔离 V9。

## 2. 目标终态

整理结束后的仓库只保留一个“当前开发路径”：

```text
冻结前的 Source 输入
  -> Cue v2 提取与事实验证
  -> trigger/lure 选择
  -> Seed 生成与人工抽查
  -> Life Log 与确定性 oracle
  -> 最终发布导出
```

当前路径的规则：

- 每个阶段只有一个当前脚本、一个当前 prompt、一个当前 Schema 和一组当前测试。
- 脚本默认以普通输入/输出文件工作，不读取 SUCCESS、manifest、旧账本或历史运行目录。
- 开发与小规模试验可以直接运行；是否调用 API 只由用户在当次对话中的明确请求和预算决定，不再要求授权 JSON、确认短语或角色审批。真实 API 调用仍必须显式传入 `--execute` 与有限正数 `--limit`，并受简单的请求数、超时、重试和成本汇总控制；去治理不等于取消运行安全。
- 输出以简单 JSONL 或 JSON 为主；除非确实需要分片，不创建 worker、package、receipt、coverage、ledger、snapshot 等中间体系。
- Schema 和配置是普通源代码，随实现一起修改并由测试保护；不再存在“冻结后只能开 CR”的规则。
- 哈希不是日常读门。开发过程最多记录输入文件名、行数、生成日期和模型名；只有外部正式发布时才可按需要写一个简单 release 说明。

## 3. 必须保留、归档和删除的边界

### 3.1 保留为当前输入

以下内容是当前研究需要的事实基础，保留在工作区，不要求哈希读门：

- `egopm_bench_v1/source/source_video_atoms.jsonl`
- Source 对应的原始 SRT 只读来源
- `egopm_bench_v1/source/source_split_map.jsonl`，若当前 split 方案仍使用它
- 当前 BGE 候选 `egopm_bench_v1/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/candidate_atoms.jsonl`（10,000 行）；它就是阶段 C/D 的正式候选输入，但仍不被当作不可修改的协议真相，允许替换、补样或重跑
- `rules/state_machine.py` 中可复用的生命周期状态机思想和测试

Source 不是“永远冻结”的圣物。若发现 SRT 解析、对齐或 split 的真实 bug，可以直接修复脚本并重新生成 Source；应在 Git 提交信息和简短开发日志中说明原因即可，不需要 CR 或旧哈希兼容层。

### 3.2 归档候选

以下内容只保留为历史复盘材料，不得出现在当前代码的默认路径、默认测试或默认文档中：

- `egopm_bench_v1/cues/formal/**`
- `egopm_bench_v1/cues/cue_library_manifest.json`
- `egopm_bench_v1/cues/CUE_LIBRARY_SUCCESS.json`
- `egopm_bench_v1/cues/realtime/**`
- 旧 V1/V3.6 prompt、旧 compact inference Schema、旧 realtime/batch 执行逻辑、旧 recovery/ledger 测试
- 历史 handoff、CR 细节、旧状态板中的波次/worker/费用叙事

归档优先使用 Git 历史：在清理前建立一个明确 tag，例如 `legacy-v9-before-cleanup`，使历史代码与已追踪的 75 个分片可恢复。被 `.gitignore` 忽略的大型 realtime 运行文件可先压缩或移动到用户指定的仓库外归档位置；确认归档可恢复后，再从当前工作区删除。

禁止为了归档而在活动仓库新建复杂 `legacy/`、`archive/`、`snapshot/` 目录树。历史代码由 Git tag 保存，历史大文件由仓库外的单个归档包保存即可。

### 3.3 可直接删除的候选

在确认没有当前消费者后，优先删除：

- 旧 realtime 的 package 文件、累计账本备份、进度文件和波次目录。
- 已被 Git tag 覆盖的 V9 formal Cue 分片及旧 Cue SUCCESS/manifest。
- `source_video_atoms_draft.jsonl`、`raw_srt_segments.jsonl` 等可由 Source 脚本重建的中间文件；若用户希望保留一次性排错材料，先移动到仓库外归档。
- 没有当前生产者和消费者的 prompt、Schema、fixture、报告、handoff、README 章节和 CHANGE_REQUEST 历史正文。
- `.pytest_cache`、`__pycache__`、临时 JSON、旧模型缓存和无消费者导出。

删除前必须执行引用盘点：对每个候选路径用 `rg` 检查当前代码、当前测试和当前文档是否仍引用；如果仍有引用，应先去除引用，再删除文件。

### 3.4 精确归档与删除清单

下表只是本次规划的起始参考，不构成足以直接删除的精确盘点。每个实质删除批次执行前，必须重新生成目标的文件数、字节数、Git 跟踪状态、当前引用和恢复方式；盘点变化时以新的结果为准。`直接删除`表示它不包含当前研究的唯一事实或当前可用代码，且须在重新盘点和用户确认后才可执行。`Git tag 后删除`只适用于已被 Git 跟踪的内容；Git tag 不能恢复未跟踪或被 `.gitignore` 忽略的文件。此类文件必须先整体提交，或先完成仓库外归档，再由用户决定是否删除。`保留`表示当前路径需要它；保留不等于建立哈希或冻结读门。

| 类别 | 当前精确路径 | 当前规模 | 处理 | 前置条件与恢复方式 |
| --- | --- | ---: | --- | --- |
| 当前 Source | `egopm_bench_v1/source/source_video_atoms.jsonl` | 约 525 MB | 保留 | 当前 Cue/Seed 的唯一事实输入；原始 SRT 仍可重建它。允许未来因真实 bug 重建，不需要继承旧 SHA。 |
| 当前 split | `egopm_bench_v1/source/source_split_map.jsonl` | 约 106 MB | 保留 | 当前 Source 行未能独立表达 split 时保留；若未来将 split 合入 Source 行，可删除此文件。 |
| Source 身份记录 | `egopm_bench_v1/source/SOURCE_ATOMS_SUCCESS.json` | 约 1 KB | 保留为非阻断 provenance 元数据 | 保留 Source 行数、来源与身份信息；新路径不得把它当作日常开发读门、哈希门或运行阻断。 |
| Source 可重建中间产物 | `egopm_bench_v1/source/source_video_atoms_draft.jsonl` | 约 534 MB | 仅阶段 G 后可直接删除 | 确认正式 Source 可读取、原始 SRT 仍在，并且正式 Cue 与 Seed 接口联调稳定后，才可重新盘点、确认并删除；可由第 01–04 步重新生成。 |
| Source 可重建中间产物 | `egopm_bench_v1/source/raw_srt_segments.jsonl` | 约 367 MB | 仅阶段 G 后可直接删除 | 确认原始 SRT 可访问，并且正式 Cue 与 Seed 接口联调稳定后，才可重新盘点、确认并删除；它只是解析缓存，不是当前下游输入。 |
| Source 小型报告 | `egopm_bench_v1/source/srt_inventory.csv`、`atom_build_report.json` | 约 176 KB | 保留为非阻断 provenance 元数据 | 分别保留来源盘点与 Atom 构建摘要，便于理解 Source 行数、来源和身份；日常 Cue 开发不得依赖它们作为读门。 |
| 当前 Cue 候选 | `egopm_bench_v1/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/candidate_atoms.jsonl` | 约 805 KB | 保留 | 它是当前 Cue v2 的初始候选列表；允许替换、补样或重跑，不将其当作冻结选择协议。 |
| 当前 BGE 五文件 | `cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/` 下的 `candidate_atoms.jsonl`、`selection_proof.jsonl`、`selection_report.json`、`selection_manifest.json`、`BGE_FILTER_SUCCESS.json` | 约 13.5 MB（删除前重盘点） | 先整体提交或仓库外归档，再由用户决定是否删除 | Cue 日常开发只读取 `candidate_atoms.jsonl`；其余四个文件不构成运行门，但 proof 的 `(sample_stratum, sample_index, atom_id)` 重复索引语义合法，不得以“`sample_index` 重复”为由判错或直接清除。当前该新路径尚未被 Git 跟踪，Git tag 不能保护它。 |
| V9 正式 Cue | `egopm_bench_v1/cues/formal/task_000.jsonl` 至 `task_074.jsonl` | 约 245 MB | 仅阶段 F、Git 恢复点后删除 | 仅在正式 Cue 与 Seed 接口联调稳定、当前引用已解除后建立 `legacy-v9-before-cleanup` tag；Git tag 可恢复已跟踪内容。 |
| V9 入口文件 | `egopm_bench_v1/cues/cue_library_manifest.json`、`egopm_bench_v1/cues/CUE_LIBRARY_SUCCESS.json` | 约 95 KB | 仅阶段 F、Git 恢复点后删除 | 仅在 05/06/07/11、配置、README 和测试的当前引用已解除后删除。 |
| 旧 API 授权 | `egopm_bench_v1/cues/CUE_EXECUTION_AUTHORIZATION.json` | 约 402 B | 仅阶段 F 后可直接删除 | 属于旧协议授权，不是研究数据；仍须确认当前代码、测试和文档已无引用。 |
| V9 realtime 全部运行 | `egopm_bench_v1/cues/realtime/runs/realtime_v9_01/wave_01` 至 `wave_08` | 最近一次盘点：376,113 文件、1,055,933,315 字节 | 仅阶段 F 后可直接删除 | 已忽略且不在 Git；只含旧请求恢复、局部结果和账本，不是当前合法 Cue 输入。正式 Cue 和 Seed 接口联调稳定后才可重新盘点、完成归档并取得用户确认；上述数字仅供识别范围。 |
| V9 realtime 根账本与备份 | `egopm_bench_v1/cues/realtime/runs/realtime_v9_01/realtime_cumulative_ledger.jsonl`、`.dup_repair_bak`、`.transient_purge_bak`、`realtime_cumulative_progress.json` | 根累计账本最近约 113.6 MB；其他文件删除前重盘点 | 仅阶段 F 后可直接删除 | 与旧 realtime 目录同批处理前，确认当前无引用，重新盘点文件数、字节数、忽略状态和恢复方式，并取得用户确认。 |
| 旧 prompt | `egopm_bench_v1/prompts/cue_extractor_v1.md`、`cue_extractor_v3_compact.md` | 约 2.5 KB | 仅阶段 F、Git 恢复点后删除 | 保留 `cue_extractor_v2.md` 作为当前 Cue prompt；先完成当前 05 的默认参数切换和新路径验证。 |
| 旧 Cue Schema | `egopm_bench_v1/schemas/cue_candidate.schema.json`、`cue_inference_batch_v1.schema.json`、`cue_inference_batch_compact_v1.schema.json` | 约 6.3 KB | 仅阶段 F、Git 恢复点后删除 | 保留 `cue_v2_inference.schema.json` 和 `cue_v2_artifacts.schema.json`；先完成 05、06、07、11 的 current Schema 切换和新链路验证。 |
| 旧执行代码 | `scripts/05_extract_cues.py` 内 V3.6 realtime/batch、八波、账本、恢复、75 分片函数，以及 900/120 验收计划函数 `preflight_bge_selection_snapshot`、`partition_cue_v2_candidates`、`_cue_v2_calibration_records`、`build_protocol_acceptance_plan` | 4,652 行文件中的绝大部分 | 阶段 C 从当前路径删除或降级；物理删除推迟到阶段 F | 阶段 C 必须让默认 CLI 与默认测试不再产生或消费 900 验收集、120 重叠、390+390 分区、510 worker 输入和 350 校准身份；不得迁入新模块、不得保留 fallback。`test_qwen_contracts.py` 中依赖它们的测试必须同批删除或继续隔离为 `legacy_v36`，不能让 `build_protocol_acceptance_plan` 成为阶段 C 的可用接口。 |
| 旧治理合同 | 根 `AGENTS.md`（当前仍为 `v1.5.0`，约 27 KB，含 T0–T4 所有权、正式启动门、SUCCESS 哈希门、工件与目录节制硬门禁） | 约 27 KB | 阶段 C 前必须由 T0 缩短；不属于阶段 F 的删除批次 | 本方案第 4 节要求它只保留简短的协作与安全要求。未缩短前，阶段 C 实现会同时受两套规则约束。同时注意 `AGENTS.md` 已把 Cue v2 的正式布局写成 `cues/v2/formal/` 加 manifest 加阶段 SUCCESS，与本方案的“单 run + checkpoint”方向冲突，必须由 T0 明确取舍。 |
| 旧测试 | `test_qwen_contracts.py`（49 条，含 900/120 验收计划测试）、`test_validators.py`（27 条 V3.6/compact 测试）、`test_contract_freeze.py`（12 条） | 默认 88 条被 deselected 的全部内容 | 阶段 C 删除验收计划测试；其余物理删除推迟到阶段 F | 阶段 C 用当前 Cue v2 端到端测试替代验收计划测试，并确认当前测试不再引用 `build_protocol_acceptance_plan` 等函数后再删除其余 legacy。当前默认套件只运行 13 条，不覆盖 validator，属于阶段 C 必须补上的空白，不是可以忽略的噪声。 |
| 旧配置残留 | `config/paths.yaml` 的 `success_markers`、`atomic_write.success_marker_required_fields`、`artifacts.cue_library_manifest`；`config/model_registry.yaml` 的 `run_manifest_required_fields` 与 `cue_v2_extraction.runs_root` | 数 KB | 阶段 C 修改，不从仓库删除 | 它们当前仍声明 SUCCESS 哈希门、manifest 必需字段和 `../cues/v2/runs` 运行根；阶段 C 必须把运行根与必需字段对齐第 5 节 C.3，或由 T0 明确保留理由。修改配置不需要 CR 或审批。 |
| 过期协调文档 | 根 `CHANGE_REQUESTS.md`、`egopm_bench_v1/coordination/CHANGE_REQUESTS.md`、`STATUS.md`、`CUE_SEED_V2_PLAN.md`、`coordination/handoffs/*.md`、旧完整生产指南 | 合计约 205 KB | 仅阶段 F、Git 恢复点后删除或压缩 | 仅在当前文档与代码引用已解除后处理；`README.md` 与本方案成为活动文档，不保留第二份状态真相。 |
| 旧审计报告 | `egopm_bench_v1/audit/validation_errors.jsonl`、`leakage_report.json` | 约 2 KB | 仅阶段 F 后可直接删除 | 它们只描述旧阶段结果；仍须确认当前实现、测试和文档无引用。 |
| 开发垃圾 | `.pytest_cache/**`、`__pycache__/**`、`egopm_bench.egg-info/**`、`*.tmp*` | 可重建 | 仅阶段 G 后可直接删除 | 测试、安装或脚本会重新创建；不归档，也不应抢在 Cue v2 稳定前清理。 |

说明：当前 `cues/v2/source_selection/...` 是未提交的路径迁移结果。实际清理不得在该路径被确认、提交或明确放弃之前删除它；尤其不能把“删除旧嵌套路径”误操作为删除当前候选文件。实测未提交状态为：旧嵌套路径 `cues/v2/v2/source_selection/bge_m3_source_select/` 下五个文件已在工作区标记为删除（`D`），新路径 `cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/` 下五个文件为未跟踪（`??`），行数 10,000。这两组变动都属于用户未提交改动，清理时必须原样保留，不得 reset、checkout 或覆盖。

### 3.5 归档与删除批次

清理不是当前路径重构的前置条件。Codex 必须遵守以下唯一顺序，不得对整个 `egopm_bench_v1/` 或 `cues/` 运行宽泛递归删除：

1. **建立可恢复点与引用盘点，不删除（已完成）**：已保护当前未提交改动；已为已跟踪的历史确定 Git 恢复点 `stage-a-recovery-20260915-01`；ignored/未跟踪的 realtime 大文件仍待确定整体提交或仓库外归档方式。对所有候选对象的引用盘点在每次实质删除前仍需按当前状态重做。
2. **完成新 Cue v2 当前路径与测试，不删除（部分完成）**：默认入口已解除对 V9 的依赖，`05_extract_cues.py` 已只做 Cue v2 只读预检。单一 `run_id` 的 `--execute`、checkpoint/finalize 输出与相关测试属于阶段 C，尚未完成。
3. **完成正式 Cue 生产器与首批正式确认，不删除**：对正式候选的前约 50 条使用正式 `run_id`、checkpoint 和输出接口，确认没有系统性错误后保留结果。同时删除或降级 900/120 验收计划设计。
4. **用同一 run 完成全部正式 Cue，不删除**：从 checkpoint 继续处理 `candidate_atoms.jsonl` 的全部候选。最终文件必须完整覆盖候选集合，且没有未完成状态。
5. **完成 Seed 接口联调，不删除**：只有第 4 步完成后，06/07 才可读取该次正式 Cue 输出并生成 Seed 小样本。
6. **归档/删除旧 V9 与 runtime**：仅在第 1–5 步完成后，归档或删除 V9 formal、旧入口、旧 prompt/Schema/测试、`realtime_v9_01` 的 `wave_01` 至 `wave_08`、根账本与备份。删除前重新盘点；最近记录的 376,113 文件、1,055,933,315 字节和约 113.6 MB 根账本只用于识别范围，不是执行依据。
7. **处理 BGE provenance（可选）**：Cue v2 日常运行只读取 `candidate_atoms.jsonl`，不把 proof、report、manifest 或 SUCCESS 当作读门。当前 BGE 五文件路径未被 Git 跟踪，必须先整体提交或仓库外归档；Git tag 不能保护未跟踪或 ignored 文件。完成其中一种保护方式后，由用户决定是否删除非候选元数据，不能自动清除 proof。
8. **最后才考虑可重建 Source 中间文件与缓存**：只有正式 Cue、Seed 接口联调和旧 V9 处理均完成后，才可考虑删除 draft、raw segments、缓存与临时文件。Source 中间文件不增加当前代码复杂度，不能在 Cue v2 稳定前优先清理；`SOURCE_ATOMS_SUCCESS.json`、`srt_inventory.csv` 与 `atom_build_report.json` 继续保留为非阻断 provenance 元数据。

任何仍被当前代码、测试或文档引用的对象不得删除。每个实质删除批次前，Codex 必须向用户展示重新生成的精确路径列表、文件数、总字节数、Git 跟踪状态、当前引用、恢复方式和拟执行动作；取得用户确认后才可操作该批次，并在完成后报告实际删除量和恢复位置。

## 4. 日常开发的新规则

从本方案实施开始，Codex 必须遵守以下简化规则：

1. 不再把 `AGENTS.md` 作为数据发布合同。它只保留简短的协作与安全要求：中文 Markdown、保护密钥、事实/constructed 边界、不得覆盖用户未提交修改。
2. 删除 T0–T4 所有权、正式启动门、工件预算、CR 编号、阶段 DONE、强制 handoff、SUCCESS 读门和“配置/Schema 不可改”的要求。**注意：这条目前只是规则，还没有落地。** 工作区中的 `AGENTS.md` 仍是 `v1.5.0` 完整合同，上述要求全部还在，且其“工件与目录节制硬门禁”会直接约束阶段 C 的 checkpoint 与正式产物。未完成这项前，本节第 2、4、5 条与 `AGENTS.md` 会同时生效，这是冲突而不是已达成状态。
3. 不再维护多份状态真相。迁移期间活动文档为根 `README.md` 与本执行方案；迁移完成后，长期研究约束应收敛为 `README.md` 加一份简短 `DESIGN.md`，进度用 Git 提交和当前 issue/对话表达。
4. 不再为开发输出计算或检查 SHA256。若最终公开发布需要可复现说明，仅在最终导出时记录源文件名、行数、生成日期、模型和 Git commit；SHA256 可选，不作为运行前提。
5. 不再为单次 API 调用创建 package、worker、lease、receipt、coverage、累计 ledger 或多层 manifest。需要恢复时只保留同一 `run_id` 的 `cues.checkpoint.jsonl`，记录 `run_id`、Atom ID、状态、尝试次数、模型 ID 和错误码即可；单账户顺序运行优先，多账号并行仅在确有吞吐需求时加入。
6. 不再把 BGE 的候选排序、聚类、CP-SAT 最优性或 proof 格式当成事实正确性证明。BGE 只负责给 Cue 提供候选 Atom；事实正确性只由 Cue 的原文证据验证决定。
7. 每次修改只要求相关测试通过。默认 `python -m pytest -q` 只运行当前流水线测试；历史测试不能成为当前开发阻断。

## 5. 代码重构路线

### 阶段 A：建立可恢复点与引用盘点

1. 检查 `git status --short`，记录并保留当前未提交改动；不得 reset 或 checkout。
2. 将当前未提交改动整理成一个用户可审阅的提交，或导出一个 patch 备份。此步骤只保护工作，不改变代码行为。
3. 为未来可能删除的已追踪 V9 代码和数据建立 Git 恢复点；对 ignored 的 realtime 数据列出精确路径、文件数、字节数和目标归档方式。此时不删除任何数据或代码。
4. 删除或大幅缩短 `AGENTS.md` 中的流程治理条款，先解除当前对话的无谓限制。

阶段 A 的完成条件：工作区有可恢复点；已完成候选清理对象的引用盘点；用户已确认历史大文件的潜在归档策略；未删除 Source、V9 或 runtime。

**当前状态（2026-09-16）：已完成，但第 4 项未做。** 恢复点 tag `stage-a-recovery-20260915-01` 已建立。`AGENTS.md` 仍是 `v1.5.0` 完整合同，第 4 项的“删除或大幅缩短”尚未执行；该项不能等到阶段 F，因为阶段 C 的实现会直接受到它的 SUCCESS 哈希门、工件预算和目录节制硬门禁约束。它已列入第 5 节末的合同冲突清单。

### 阶段 B：建立当前 Cue v2 路径并隔离旧 V9

1. 在 `05_extract_cues.py` 建立小而直接的 Cue v2 当前入口，移除旧 V3.6 realtime/batch、八波、恢复、ledger和 75 分片作为默认 CLI 路径；旧实现暂时保留为不被当前入口、测试或文档调用的历史代码，物理删除推迟到阶段 F。
2. 仅保留 Cue v2 需要的内容：读取 Atom、构造结构化请求、解析响应、执行三层事实验证、写出当前 Cue JSONL。
3. 删除旧 `cue_extraction` 配置，只保留一个简短的 `cue_v2_extraction` 配置文件，统一存放 Source、候选、输出、模型和运行参数的默认值；CLI 可以显式覆盖其中字段，脚本内不得再硬编码第二套默认路径，也不再并行维护 `paths.yaml` 的 Cue 默认值。
4. 将默认 prompt 与 inference/final Schema 切换到当前 Cue v2；旧 prompt 与 Schema 暂时保留为不被当前路径调用的历史对象，物理删除推迟到阶段 F。
5. 仅从当前 README、状态板、测试和脚本中移除对 V9 formal、旧 Cue manifest/SUCCESS 与旧 realtime 的默认引用；物理归档或删除必须等到阶段 E 的 Seed 接口联调完成之后。

阶段 B 的完成条件：当前 CLI、当前测试和 README 不再默认引用 `realtime_v9_01`、`legacy_v36`、`worker_02`、`cue_extractor_v3_compact`、75 分片或旧 Cue SUCCESS；这些文本在阶段 F 前可暂留于明确隔离且不被当前路径调用的历史代码或说明中。

**当前状态（2026-09-16）：核心已完成。** 默认入口只做 Cue v2 只读预检，默认不读 realtime、manifest、SUCCESS 或 proof；`--legacy-v9` 已改为直接报错退出；默认 `02`/`03` 不再指向旧 prompt 或旧 Schema。仍遗留三点，均已在第 5 节末登记：`pyproject.toml` 仍以 `addopts = "-ra -m 'not legacy_v36'"` 引用 `legacy_v36` 标记；`06`/`07`/`11` 的默认参数仍指向 `cue_library_manifest` 与 `cue_candidate.schema.json`（属于阶段 E）；`05` 内 900/120 验收计划代码与其 legacy 测试仍存在于同一文件与同一测试模块（属于阶段 C）。

### 阶段 C：完成正式 Cue 生产器并启动首批正式生产

阶段 C 的目标是尽快得到一个简单、可恢复、能够处理完整 10,000 Atom 候选池的**唯一正式 Cue 生产器**，并让它直接启动正式生产。唯一入口是 `egopm_bench_v1/scripts/05_extract_cues.py`；唯一模型配置是 `egopm_bench_v1/config/model_registry.yaml` 中的 `cue_v2_extraction`。旧 V9 只可作为隔离历史，不能成为 fallback、输入或默认路径。

1. 直接以 `candidate_atoms.jsonl` 中正式的 10,000 条 BGE 候选 Atom 为输入；日常运行只读候选与对应 Source Atom，不读 proof、manifest、SUCCESS、旧 realtime 或旧 Cue。
2. 一个生产器统一支持六类 Cue dimension；不得按 dimension、测试规模或 acceptance 另建运行阶段、目录树或输出接口。
3. 少量 synthetic fixture 仅用于离线检查请求构造、响应解析、Schema、span 验证、checkpoint 与恢复。它不是独立数据流程，也不产生可供下游使用的 Cue。
4. 尽早接通 API。API 接通后，按候选文件的确定性顺序处理首批约 50 条；这称为“首批正式确认”，不是 smoke，也不是独立验收 campaign。
5. 首批 50 条必须使用正式的模型、prompt、Schema、参数、`run_id`、checkpoint 与输出接口。确认通过后保留这些结果，并从第 51 条在同一 `run_id` 继续；不得为已确认数据另建或重生一套正式 Cue。
6. 若首批发现必须修改 prompt、Schema、模型或关键采样/允许矩阵参数，当前 `run_id` 整体失效；创建新 `run_id` 并从头处理，禁止混入两套条件下的结果。
7. 阶段 C 不启动 Seed 正式生产；`06`/`07` 只能在本阶段使用 synthetic fixture 或明确标记的非正式样例做接口准备，不能读取 checkpoint、局部输出或首批正式结果。

首批正式确认的人工检查只针对会导致系统性数据错误的项目：输入输出映射；`accepted_cue`、`no_cue`、`excluded_ambiguous` 分流；`evidence.field` 与连续 span；value 的直接支持；人物角色、否定、范围、条件/假设与时间语气；以及 checkpoint 恢复是否造成重复或遗漏。它不建立独立验收运行、专用目录或额外治理工件。

阶段 C 的完成条件不是“完成 50 条”，而是：正式生产器已可用；首批正式确认未发现已知系统性错误；同一个有效 `run_id` 能从 checkpoint 无缝继续其余 Atom；并且没有已知的输入映射、事实验证或恢复逻辑缺陷。完成 50 条、100 条、500 条或任何部分数量，在后续阶段都只是正式生产进度，不能单独宣布 Cue 阶段完成。

#### 阶段 C 必须删除或降级的设计

以下设计在阶段 C 一律不得继续存在、不得被默认路径调用、不得被当作可用接口。表内同时给出它们在当前仓库中的确切位置，便于删除或降级时逐项核对；这些位置都已实测确认，不是预留设计。

| 必须删除或降级的设计 | 当前确切位置 | 处置 |
| --- | --- | --- |
| 900 条独立验收集 | `05_extract_cues.py::build_protocol_acceptance_plan`（`acceptance_atom_ids`，`len(selected) != 900` 直接抛错） | 从当前路径删除；不得降级为可选参数 |
| 120 条两账号大规模重叠 | 同上（`overlap`、`overlap_atom_ids`、`ACCEPTANCE_OVERLAP_INVARIANT_FAILED`） | 删除 |
| 350 条按 stratum 的校准身份 | 同上及 `_cue_v2_calibration_records`、`_CUE_V2_CALIBRATION_STRATA`、`_CUE_V2_CALIBRATION_SAMPLES_PER_STRATUM`、`_CUE_V2_CALIBRATION_IDENTITY_VERSION`、`_CUE_V2_CALIBRATION_INDEX_RULE` | 删除；proof 的 `audit_sample` 行退回为非阻断 provenance |
| 350 → 900 → 10,000 多层前置门 | 同上（`ACCEPTANCE_POOL_TOO_SMALL`、`ACCEPTANCE_CANDIDATE_COUNT_INVALID` 要求候选恰为 10,000 且剩余池 ≥ 900） | 删除；候选数只作为输入范围校验 |
| 独立 acceptance campaign | `preflight_bge_selection_snapshot`、`build_protocol_acceptance_plan`、`test_qwen_contracts.py` 中约 30 条验收计划测试 | 从当前路径与当前测试删除 |
| 为验收专设的 worker / package / staging / ledger / manifest / SUCCESS / 分布报告 | `partition_cue_v2_candidates`（`worker_00`/`worker_01`）、`worker_acceptance_inputs`、`worker_partitions`、`plan_sha256` 等一系列 `*_sha256` 身份字段 | 删除；阶段 C 只保留单 run checkpoint 与最终 `cues.jsonl` |
| 800–1,000 条协议验收 Atom 与约 500 条受控子波次 | `egopm_bench_v1/coordination/CUE_SEED_V2_PLAN.md`（“先完成人工标注的 350 条设计校准，再冻结 800–1,000 个协议验收 Atom，其中至少 120 个由两个账号重叠处理；验收先跑约 500 条不改协议的受控子波次”） | 随该协调文档在阶段 F 删除或压缩；阶段 C 不得按它实施，也不得据此重建 20/100/500 式逐级放行门 |
| 按 dimension 分阶段开放 API | `cue_v2_inference.schema.json` 的 `non_accepted.disposition` 含 `unsupported`；`cue_v2_artifacts.schema.json` 的 `base.status` 含 `needs_review`、`deferred_unsupported`；`test_cue_v2_current.py` 用断言锁定这两个值 | 降级为三种正式终态 `accepted_cue` / `no_cue` / `excluded_ambiguous`；同步去掉锁定旧状态的测试断言 |
| 为分阶段开放能力而产生的临时正式状态 | 同上，以及 `reason_codes` 中的 `EXPLICIT_TIME_NOT_ENABLED`、`UNSUPPORTED_RELATION` | 不再作为正式状态存在；无法安全接受的候选直接写 `excluded_ambiguous` |
| 与 Cue 验收无关的 Seed 候选规模叙事 | 根 `AGENTS.md` 第 58 段与 `coordination/CUE_SEED_V2_PLAN.md` 都写 Seed candidates 为 900–1,400 | 与本方案的 Cue 阶段边界无关；由 T0 在缩短 `AGENTS.md` 与处理 `CUE_SEED_V2_PLAN.md` 时一并决定，阶段 C 不据此改变 10,000 Cue 候选的处理范围 |
| 把完成 100 / 500 条误认为可以进入 Seed | `coordination/CUE_SEED_V2_PLAN.md` 中的多层前置门叙事，以及任何按数量放行的脚本常量 | 取消；阶段边界只按产物划分——Cue 阶段以 10,000 条全部取得唯一终态并生成闭合正式 Cue 为界，Seed 不得在此之前开始 |
| 围绕测试建立正式目录和主代码结构 | 900/120 验收计划的 `worker_partitions`、`worker_acceptance_inputs`、按规模或 dimension 建目录的旧口径 | 取消；正式目录只服务单 run 的 checkpoint 与最终 `cues.jsonl`，测试只用文件内固定样例，不建独立数据目录 |

删除不等于要重写历史：这些函数在阶段 C 从默认路径与默认测试移除后，可以按第 3.4 节推迟到阶段 F 才物理删除。但它们不得在阶段 C 期间保持“可被 import、可被调用、可由测试发现”的状态。

#### 阶段 C 技术实施说明：一条正式生产路径，先确认再连续完成

技术实现围绕同一条正式路径：给定一个 Source Atom，生成一个可由该 Atom 原文直接支持的短观察条件，或给出确定的非接受终态；将结果追加到同一 `run_id` 的 checkpoint；在中断后继续；最终整理为稳定正式输出。首批 50 条只是这条路径的首次人工确认，不改变其输入、协议、输出或下游边界。

##### C.1 运行范围与输入

正式生产器始终消费以下三个输入，并按候选文件的确定性顺序覆盖全部 10,000 Atom：

| 输入 | 路径/形状 | 用途 | 不应读取的内容 |
| --- | --- | --- | --- |
| Source Atom | 单一 `cue_v2_extraction` 配置解析出的 `source_video_atoms.jsonl` | 提供 `atom_id`、`transcript`、`dense_caption`、必要的 participant 元数据 | SUCCESS、旧 Cue、旧账本、视频时间戳、`visible_text` |
| 候选 ID | 单一 `cue_v2_extraction` 配置解析出的 `cues/v2/source_selection/.../candidate_atoms.jsonl` | 决定本次处理哪些 Atom，保持文件给出的顺序 | proof、report、manifest、CP-SAT/cluster 细节 |
| 当前 prompt 与 inference Schema | 单一 `cue_v2_extraction` 配置指定的 `prompts/cue_extractor_v2.md`、`schemas/cue_v2_inference.schema.json` | 约束模型只输出结构化候选 | 旧 V1/V3.6 prompt 与 compact Schema |

读取 Source 时采用一次流式扫描：先把候选 `atom_id` 放入集合，再扫描 Source JSONL，只保留命中的 Atom。启动 API 前必须检查候选 ID 唯一、每个候选都在 Source 中找到、`transcript`/`dense_caption` 至少一个非空。缺失 ID 是本地输入错误，应立即停下并修复候选输入；不应发 API 请求后再依靠账本补救。

日常 Cue 运行不读取 `selection_proof.jsonl`，只以 `candidate_atoms.jsonl` 和对应 Source Atom 作为输入。proof 的 `sample_index` 必须按 `(sample_stratum, sample_index, atom_id)` 理解；它与缺失的原始 artifact Schema 都只属于非阻断 provenance，不能决定一个 Cue 是否真实，也不能据此把 proof 判错或删除。

##### C.2 将 05 保持为唯一的小型正式脚本

`scripts/05_extract_cues.py` 不再分出验收脚本或测试生产器。它的职责只包括以下七件事：

1. 解析 CLI 与唯一的 `cue_v2_extraction` 配置；CLI 覆盖值优先，禁止再从脚本硬编码或第二份路径配置取得默认值。
2. 载入候选 Atom。
3. 按小批次构造模型请求。
4. 调用模型或在 `--dry-run` 下打印请求摘要。
5. 解析结构化响应并核对返回 item 与输入 item 一一对应。
6. 执行本地三层事实验证。
7. 追加最少 checkpoint，并在全量闭合后生成正式 Cue JSONL 与总计数。

当前 Cue v2 路径不应调用 batch 文件、八波、实时进度、worker、租约、授权 JSON、费用快照、恢复状态机、75 分片、manifest、SUCCESS 或旧 Cue 合并函数，也不应调用 `preflight_bge_selection_snapshot`、`partition_cue_v2_candidates`、`_cue_v2_calibration_records` 或 `build_protocol_acceptance_plan`。不要把这些旧函数迁入新模块；在阶段 F 前仅隔离为不被当前路径调用的历史代码，阶段 F 经恢复点、引用盘点和用户确认后再删除。900/120 验收计划代码是阶段 C 的直接清理对象，详见上文“阶段 C 必须删除或降级的设计”。

建议的普通 CLI 形状：

```powershell
# 不联网：仅检查正式输入、请求构造与本地验证逻辑
python egopm_bench_v1/scripts/05_extract_cues.py --dry-run --run-id cue-formal-20260916-a --limit 50

# 首批正式确认：直接处理正式候选的前约 50 条
python egopm_bench_v1/scripts/05_extract_cues.py --execute --run-id cue-formal-20260916-a --limit 50 --max-requests 50 --timeout-seconds 60 --max-cost-cny 20 --batch-size 1

# 通过首批确认后，在同一 run 继续剩余候选；已终态 Atom 不会重发
python egopm_bench_v1/scripts/05_extract_cues.py --execute --run-id cue-formal-20260916-a --resume --limit 10000 --max-requests 9950 --timeout-seconds 60 --max-cost-cny 200 --batch-size 5

# 全部候选取得终态后，整理同一 run 的权威输出
python egopm_bench_v1/scripts/05_extract_cues.py --run-id cue-formal-20260916-a --finalize
```

`--execute` 只是普通 CLI 开关；真正的外部 API 调用仍以用户在当次对话中明确同意为准。不要再要求 `START_*` 确认短语或本机授权 JSON。真实执行时，脚本必须拒绝缺少有限正数 `--limit` 或 `--max-requests` 的命令，绝不默认无限量处理候选；`--max-requests` 是本运行的硬上限，达到即正常停止并保留 checkpoint。`--max-cost-cny` 是可选但推荐的简单成本上限，达到即停止后续请求；单次超时由 `--timeout-seconds` 控制。`--batch-size` 只允许 `1..5`，首批正式确认可固定为 `1`，确认后可按实际吞吐提高到 `3` 或 `5`，不因此切换生产器或输出格式。

##### C.3 一个运行对应一对轻量文件，避免静默混批

一个最终 `cues.jsonl` 只代表一个 `run_id`，每个候选 Atom 在该最终文件中恰好一条当前终态。正式路径以 `egopm_bench_v1/cues/v2/formal/` 为根；实施前仅核对并确定真正需要的最小集合，例如同一 `run_id` 的 `cues.checkpoint.jsonl` 与最终 `cues.jsonl`。不得预建按测试规模、dimension、package、worker 或单次 attempt 划分的子目录，也不得建立第二套目录树。

目录名在实施前需与仓库现状对齐一次：`config/model_registry.yaml` 的 `cue_v2_extraction.runs_root` 当前写的是 `../cues/v2/runs`，而 `AGENTS.md` 已把 Cue v2 正式布局写成 `cues/v2/formal/` 加正式分片、manifest 和阶段 SUCCESS。阶段 C 必须选定一个名字并只改这一处配置，`cues/v2/runs` 目录当前并不存在，不需要为它建立兼容层。无论选哪个名字，都只保留最少恢复状态（同一 `run_id` 的 `cues.checkpoint.jsonl`）和最终权威产物（同一 `run_id` 的 `cues.jsonl`），不因选名而引入 manifest、分片或 SUCCESS。

“最少恢复状态”与“最终权威产物”的区别必须保持清楚：`cues.checkpoint.jsonl` 是逐行追加的运行期恢复依据，可以包含同一 Atom 的多次尝试；`cues.jsonl` 只能由同一 checkpoint 去重、校验后原子生成，运行中绝不追加。除这两个文件外不为本次运行建立第三类工件。

当 prompt、推理 Schema、模型 ID、采样参数，或允许的 `operator`/`dimension` 矩阵发生变化时，必须新建 `run_id`。`run_id` 可以由用户指定，也可以由启动时间生成；不需要逐行 SHA256。该规则只防止同一数据文件静默混入不同实验条件，不是冻结、审批或哈希读门。

每条结果至少记录 `run_id`、`atom_id`、`status`、`attempt`、`model_id`、`reason_codes`；只有 `accepted_cue` 才有 `cue`。`cue_id` 使用运行隔离的稳定形式 `cue_<run_id>_<atom_id>`，不从内容计算 hash。每个 accepted clause 还必须有明确的 `anchor`：它说明谁或什么承载状态、执行活动、持有物体或位于地点，且与 `value` 一样可由同一 evidence span 直接支持。例如 checkpoint 或最终文件的一行可以是：

```jsonl
{"run_id":"cue-formal-20260916-a","atom_id":"src_000123","status":"accepted_cue","attempt":2,"model_id":"qwen3.7-flash","cue":{"cue_id":"cue_cue-formal-20260916-a_src_000123","predicate":{"all_of":[{"dimension":"object","operator":"present","anchor":{"role":"observed_object","value":"手机"},"value":"手机","evidence":{"field":"dense_caption","span":"手机放在桌上"}}]}},"reason_codes":[]}
{"run_id":"cue-formal-20260916-a","atom_id":"src_000124","status":"transport_error","attempt":1,"model_id":"qwen3.7-flash","reason_codes":["HTTP_429"]}
{"run_id":"cue-formal-20260916-a","atom_id":"src_000125","status":"excluded_ambiguous","attempt":1,"model_id":"qwen3.7-flash","reason_codes":["UNRESOLVED_REFERENT"]}
```

阶段 C/D 期间 `06`/`07` 不得消费 checkpoint、局部输出或首批结果。只有完整覆盖候选池并通过最终检查的单个正式 `run_id` 才能在阶段 E 被读取；下游只使用其中 `status == "accepted_cue"` 的行，不得从多个运行中逐条挑选。若 predicate 改变，所有引用该 `cue_id` 的 Seed 和 Life Log 都必须重建，不能沿用旧引用。开发中可以直接修改 prompt、Schema 或验证代码并重跑，但必须使用新 `run_id`，不得覆盖或混入旧运行。

运行中的恢复文件与最终输出分工如下：

```text
运行中：
  cues.checkpoint.jsonl
  - 逐行追加。
  - 是断电、Ctrl+C、网络失败后的唯一恢复依据。
  - 可含同一 atom 的多次尝试；最后一条有效记录决定该 atom 的当前状态。

运行结束或显式 --finalize：
  - 读取同一 run_id 的 checkpoint。
  - 按 (run_id, atom_id) 去重，选择最后一条有效记录。
  - 校验记录结构与本次输入 Atom 范围。
  - 先写临时文件，再原子替换生成该 run_id 的 cues.jsonl。
```

`cues.jsonl` 是整理后的稳定输出，运行中绝不直接追加；`cues.checkpoint.jsonl` 是唯一允许的轻量恢复文件，不再建立 package、worker、lease、receipt、coverage 或多层 ledger。原子替换只用于保证最终文件不会留下半写入内容，与 checkpoint 的逐行恢复并不矛盾。

##### C.4 推理 Schema 与正式终态

推理响应与正式运行应只保留三种清晰终态：

```text
accepted_cue:
  required: item_index, disposition, predicate
  predicate.all_of: 1..3 clauses
  每个 clause required: dimension, operator, anchor, value, evidence

no_cue:
  required: item_index, disposition, reason_code
  禁止 predicate

excluded_ambiguous:
  required: item_index, disposition, reason_code
  禁止 predicate
```

模型响应顶层仍是 `{"items": [...]}`。`anchor` 的形状固定为 `{"role": "...", "value": "..."}`：`state:is`/`state:changes_to` 的 `anchor` 是状态承载者，例如“门”，`value` 是“打开”；`object:held` 的 `anchor` 是持有者，例如“Jake”，`value` 是被持有物，例如“手机”；活动的 `anchor` 是活动承载者，地点的 `anchor` 是被定位的实体。`object:present` 也必须显式写出被观察物体的 `anchor`，即使它与 `value` 相同。这样 `state:is + open` 和 `object:held + phone` 都不能在缺少关系承载者时被接受。

模型输出中的不明确项归入 `excluded_ambiguous`，不另设分阶段正式状态。六类 dimension 由同一个生产器支持；无法满足事实门的候选应明确成为 `no_cue` 或 `excluded_ambiguous`，而不是等待另一条能力流水线。本地解析必须检查：

- 返回项数等于输入项数；
- `item_index` 恰好覆盖 `0..n-1`，没有重复、遗漏或越界；
- 每个 `accepted_cue` 先经 JSON Schema 验证，再进入事实验证；
- `no_cue` 与 `excluded_ambiguous` 不允许偷偷携带 predicate；
- JSON 解析失败、结构不完整或有额外字段时写 `invalid_output`，不保存原始响应。

阶段 C 必须把这两份 Schema 的状态口径改到三种终态上。实测当前口径为：`cue_v2_inference.schema.json` 的 `non_accepted.disposition` 是 `["no_cue", "ambiguous", "unsupported"]`；`cue_v2_artifacts.schema.json` 的 `base.status` 是 `["accepted", "no_cue", "needs_review", "deferred_unsupported", "invalid_output", "transport_error"]`，且 `accepted` 分支要求 `cue.cue_id` 满足 `^cue_[A-Za-z0-9_-]+$`。两份文件都没有 `excluded_ambiguous`，`accepted` 也不是 `accepted_cue`。修改时以本节 C.4 的三种正式终态为准：`ambiguous`/`unsupported`/`needs_review`/`deferred_unsupported` 一律合并为 `excluded_ambiguous`；`invalid_output` 与 `transport_error` 保留为可重试运行状态，不进入正式终态集合。`cue_v2_artifacts.schema.json` 可以缩为“一行 Cue”，或让 Python 的 dataclass/显式字典与 inference Schema 共同承担结构约束；两种做法都不得重新引入 manifest、formal shard 或 SUCCESS 相关定义。

##### C.5 事实验证实现：三层检查，不引入额外治理

本地验证必须独立于模型完成，但只保留与事实正确性直接相关的检查。结构、字段选择、连续 span、`anchor`/`value` 的直接字符串支持和允许矩阵可以做确定性硬校验；人物角色、主客体、量词范围、否定作用域、条件与时态等完整断言语义不能被描述成完全确定性的本地证明，只能用保守规则过滤，再由分层人工抽查校正。

**第一层：字段和连续 span。** `evidence.field` 只能是 `transcript` 或 `dense_caption`。`evidence.span` 必须逐字连续出现在所声明的原字段中；开发初期采用严格原文子串匹配，不接受跨字段拼接、跨 Atom 拼接、释义或从 `visible_text` 借证据。严格匹配比复杂的 offset/NFKC 回填更容易调试；如果模型经常因空白或全半角失败，再只增加 NFKC 与连续空白折叠这一种确定性规则。

**第二层：anchor/value 直接支持。** 对 span、`anchor.value` 和 value 进行同一种最小规范化（NFKC、首尾去空白、连续空白折叠），要求两者都是 span 的连续子串或大小写等价子串。不得做分词、同义词、翻译、实体补全、词干化或知识推理。`"手机放在桌上" -> anchor/value="手机"` 可以；`"手机放在桌上" -> value="电子设备"` 不可以。对于 `object:held`，持有者和物体都必须可由 span 直接支持；对于 state，承载者和状态值都必须可由 span 直接支持。

**第三层：断言语义的保守过滤。** 这一层不是对人物角色、主客体、范围或时态作完全确定性的自动证明；它只检查维度、operator、anchor 角色与 value 的组合是否明显不成立，并把高风险表达归入 `excluded_ambiguous`，再由首批正式确认和全量后的少量人工抽检发现漏网错误：

- 出现未能在同一 span 内解析的代词、泛指词、无承载者活动或缺失 `anchor`，归入 `excluded_ambiguous`。
- 证据包含显式否定而 operator 是肯定，或相反，归入 `excluded_ambiguous`。不要用简单字符匹配把 `notebook` 误判为英文 `not`。
- 人物被提到、人物说话与人物在场是三个不同断言。Source 没有明确角色时，不接受 `person:present` 或 `person:speaking`。
- 地点必须由“在/进入/离开某地”等明确位置表达支持；物体出现在画面或文本中不能反推地点。
- `event_timestamp`、session 时间和行号永远不能形成 `explicit_time` Cue；含有假设、条件、未来计划或不确定语气且无法明确支持的表达归入 `excluded_ambiguous`。
- 两个或三个 clause 必须都各自通过前两层，且共同描述同一观察；不能为了提高丰富度把“厨房、手机、洗碗”拼成三个无关条件。

##### C.6 六类 dimension 的统一支持

`person`、`location`、`object`、`activity`、`state` 与 `explicit_time` 由同一个正式生产器、prompt、Schema 和 `run_id` 处理。不得为每个 dimension 开辟测试阶段、能力开关、单独目录或等待状态。每个候选仍须独立通过三层事实门；当某个维度在给定证据下不能安全成立时，输出 `no_cue` 或 `excluded_ambiguous`，而不是把问题推迟给另一轮正式生产。未来提醒时间继续由 Seed/Life Log 的 constructed 事件表达，Source 的 `event_timestamp` 不得成为 Cue 条件。

##### C.7 网络错误与轻量恢复

阶段 C 只需要 checkpoint 加最终原子整理这一套可解释恢复规则。`--resume` 必须指定 `run_id`，只读取和追加同一 `run_id` 的 `cues.checkpoint.jsonl`；它绝不恢复、覆盖或挑选另一运行的结果。

真实 API 执行的最低安全规则如下。这些规则处理成本与运行风险，不构成审批、冻结或治理读门：

- 只有同时显式给出 `--execute`、有限正数 `--limit` 和有限正数 `--max-requests` 才能联网；任一缺失时 CLI 必须拒绝执行，不能默认处理全部候选。
- `--max-requests` 是本次运行的最大模型请求数，达到后正常停止；建议同时给出 `--max-cost-cny` 作为简单的费用上限。若接口返回的累计费用达到该上限，立即停止发起后续请求。
- 单请求必须有有限 `--timeout-seconds`，默认建议 60 秒；网络超时、429、5xx 最多重试两次，不做无限退避或自动扩量。
- 运行中在内存/控制台汇总已发请求数、成功数、失败数、输入/输出/总 token 与累计费用；接口未返回 usage 或费用时明确显示“不可用”，不能虚构数值。结束、达到上限、Ctrl+C 或异常后都输出一次简短汇总。
- checkpoint 的每行可附带该次 `usage` 和估算费用，作为恢复与汇总依据；不额外创建费用快照、package、worker、lease、receipt 或复杂 ledger。

| checkpoint 当前状态 | `--resume` 行为 | 说明 |
| --- | --- | --- |
| `transport_error` | 自动重试，最多再试两次 | 网络超时、429、5xx 等可恢复传输失败；每次追加一条更高 `attempt` 的记录。 |
| `invalid_output` | 可用 `--retry-failed` 在同一 `run_id` 再试一次 | 仅适用于未改变 prompt/Schema/模型/采样/矩阵的修复性重试；若这些条件变化，改用新 `run_id`。 |
| `accepted_cue` | 默认跳过 | 已是有效终态；若需要改 predicate 或事实验证规则，使用新 `run_id`，并重建其下游引用。 |
| `no_cue` | 默认跳过 | 仅表示当前运行条件下未找到可接受 Cue，不等价于永久没有 Cue。 |
| `excluded_ambiguous` | 默认跳过 | 证据或完整断言语义不足以安全接受；若后续修改了 prompt、Schema、模型或验证规则，必须使用新 `run_id` 从头处理。 |

finalize 时，脚本只对同一 `run_id` 的 checkpoint 按 `(run_id, atom_id)` 选择最后一条有效记录；`transport_error` 和未完成的 `invalid_output` 不得被误写成已完成的 `no_cue`。随后校验每个记录的字段、`run_id` 与本次输入 Atom 范围，再原子生成稳定 `cues.jsonl`。Ctrl+C 或异常不会触发 finalize；已追加的 checkpoint 仍可由同一 `run_id` 的 `--resume` 继续。只有三种正式终态齐全时才能闭合输出。

单个 API 调用的用量只做运行期与结束时汇总，不写 worker ledger、费用快照或 request hash。若用户要停止，脚本正常退出；已追加的 checkpoint JSONL 行就是恢复点。

##### C.8 首批正式确认与人工反馈

联网前可用少量 synthetic fixture 覆盖请求构造、解析、连续 span、错误响应与 checkpoint 恢复；它们只检查代码，不决定数据是否可用。

首批 50 条不是“smoke”，也不是一套独立测试或独立数据目录。它就是正式生产的一部分：输入是正式 10,000 候选的前 50 条，使用正式的模型、prompt、Schema、参数、输出目录、checkpoint 和同一个 `run_id`，结果与其他 9,950 条同处一份正式数据中。

首批确认只按以下五项检查，不逐条做完整人工标注：

- 请求与返回的映射有没有错：返回 item 数等于输入 item 数，`item_index` 恰好覆盖 `0..n-1`，没有串位、重复或遗漏。
- `accepted_cue` 与 `no_cue` 的比例是否明显异常：例如在一个明显充满可观察条件的样本上几乎全 `no_cue`，或在一个几乎没有可观察事实的样本上大量 `accepted_cue`，都属于需要先查清的系统性信号。比例只用于发现异常，不作为命中率指标，也不设阈值门。
- `evidence.span` 是否真实：必须是所声明字段中的逐字连续原文，跨字段、跨 Atom 或虚构实体为零错误。
- 人物角色、否定、范围、条件与时间语气有没有系统性错误：同一种误读在 50 条里重复出现，就算系统性错误。
- checkpoint 与恢复是否正常：中断后 `--resume` 不重复已终态 Atom、不遗漏未处理 Atom，且不重复计费。

若需修改 prompt、Schema、模型或关键参数，该 `run_id` 整体作废，另开 `run_id` 从候选开头重启。失败结果不得迁移到新 run，也不得手工编辑 checkpoint 或最终 JSONL 使其“看起来正确”；`no_cue`、`excluded_ambiguous` 与任何可重试状态都不能靠手改变成 `accepted_cue`。若未发现此类系统性问题，保留首批结果并立即从第 51 条继续。人工检查是反馈循环，不是审批或多层放行门。

##### C.9 阶段 C 的必要测试

测试只保护正式生产器免于明显错误，不组成独立流水线。离线测试的准备方式是：在测试文件内准备少量固定的 synthetic 样例，检查请求能否正确构造、返回值能否正确解析、三种终态能否正确区分、连续 span 与字段引用能否验证、以及中断后是否既不重复计费也不漏数据。这些测试不调用 API，不需要单独的数据目录或 fixture 目录树，也不产生任何可供下游使用的 Cue。数量不重要，覆盖关键错误即可；不要为了凑测试规模再建一套样例池。

最小测试集应覆盖：

- 推理 Schema 的三种正式终态；
- item 数量和 `item_index` 的一一对应；
- 同字段连续 span、禁止 `visible_text`、禁止跨 Atom；
- `anchor`/value 直接支持及 Unicode/空白边界；缺失状态承载者、持有者或活动承载者必须拒绝或待审；
- 否定、人物角色、主客体、地点推断、条件/未来时间、无法解析代词；
- 六类 dimension 的允许 operator 与不成立断言；
- 同一 `run_id` 的 checkpoint 逐行追加、`--resume`、`--finalize` 去重原子整理，以及不同 `run_id` 不混批；
- 中断后不重复计费、不遗漏 Atom：模拟在写入 checkpoint 前后中断，确认已终态 Atom 不会被重新计费，未处理 Atom 不会被跳过；
- fake transport 的端到端小批次，不发真实 API；
- 正式生产完成度检查：候选集合必须完整覆盖，且不得遗留 `transport_error`、`invalid_output` 或其他非正式终态。

不再测试旧 75 分片、八波、旧 authorization、旧成本熔断、CP-SAT proof、worker 账户一致性或历史账本恢复，也不再测试 900 条验收集、120 条重叠、390+390 分区、510 worker 输入、350 条校准身份或 `plan_sha256` 一类的验收计划身份。`test_qwen_contracts.py` 中依赖 `build_protocol_acceptance_plan` 与 `preflight_bge_selection_snapshot` 的测试必须随验收计划一起删除，不能继续留在默认测试里。

当前默认测试只有 13 条且完全不覆盖 validator；阶段 C 在替代验收计划测试时，应把三条正式终态、三层事实门和 checkpoint 恢复的验证器覆盖补进默认套件，避免“默认全绿”只是因为 legacy 被整体 deselected。

##### C.10 阶段 C 完成判据

阶段 C 完成不是“拿到某个 SUCCESS 文件”，而是正式生产器能够读取正式候选、发出受限 API 请求、解析并验证响应、逐行写 checkpoint、在中断后恢复并最终化；首批约 50 条正式结果已完成针对系统性错误的人工确认；同一有效 `run_id` 能无缝继续剩余候选；06/07 没有读取 checkpoint、局部输出或首批正式结果。

达到这些条件后，直接连续处理剩余候选。不存在额外的规模放行门；任何部分完成量只报告进度。若出现系统性问题，修改实现后创建新 `run_id` 并从头开始，不拼接不同条件的结果。

##### C.11 正式生产循环与阶段边界

同一个入口从第 1 条跑到第 10,000 条，中间不切换程序。每次迭代固定为同一条循环：

```text
读取候选
  → 跳过已有终态的 Atom
  -> 小批调用 API
  -> 本地三层事实验证
  -> 追加 checkpoint
  -> 失败有限重试
  -> 继续下一批
  -> 全部候选取得终态后生成正式 Cue
```

首批 50 条只是这条循环的前 50 次迭代；第 51 条到第 10,000 条使用同一入口、同一 `run_id`、同一 checkpoint 和同一输出目录。不存在“阶段 D 程序”，也不存在为后面 9,950 条单独准备的第二种数据格式。

阶段边界按产物划分，而不是按处理数量划分：

```text
Cue 阶段：10,000 个候选 Atom 全部取得唯一终态，并生成闭合的正式 Cue 产物
Seed 阶段：只在 Cue 阶段完整结束后才开始
```

因此完成 50、100 或 500 条都只是 Cue 阶段的执行进度，不能触发 Seed，也不能被当作“Cue 已完成”。如果仍要保留 `CUE_LIBRARY_SUCCESS` 之类的闭合标记名字，它只能是一个说明“10,000 条终态齐全、`cues.jsonl` 已闭合”的最小标记，不得重新引入哈希链、账本、分片清单或阶段 SUCCESS 读门；这个名字与 `AGENTS.md` 既有 SUCCESS 口径的取舍由 T0 一并决定，见本节阶段 D 之后的“阶段 C/D 实施前需要由 T0 处理的合同冲突”。

### 阶段 D：用同一正式生产器完成全部 10,000 Atom

阶段 D 不是另一套程序、另一种数据格式或独立测试系统。它使用阶段 C 的同一个 `05_extract_cues.py`、同一份正式模型/prompt/Schema/参数和**同一个仍然有效的 `run_id`**，从首批正式确认的 checkpoint 接着处理第 51 条及其余候选。阶段 C 的首批结果属于正式数据，确认通过后必须保留；不得为了“进入阶段 D”另开运行、复制目录或重算前 50 条。

1. 以 `candidate_atoms.jsonl` 的确定性顺序处理完整 10,000 Atom。`--resume` 只读取该 `run_id` 的 checkpoint，并跳过已有有效终态的 Atom；它不得重新发送、覆盖或从其他 run 抄取结果。
2. 运行中的可重试状态仅包括网络错误、限流、超时和格式错误。它们不是数据终态；每种错误只允许有限次数重试，超过上限必须停止并报告，绝不无限重发或把失败伪装成 `no_cue`。
3. 正式运行内部终态保持三种：`accepted_cue`、`no_cue`、`excluded_ambiguous`。完成时每个 Atom 必须取得且仅取得其中一个终态；checkpoint 中的可重试状态必须清空或由新的有效记录替代后，才能 finalise。
4. 每条 `accepted_cue` 必须逐条通过确定性验证：Source Atom 存在；`evidence.field` 合法；span 是该字段的连续原文；value 可由 span 直接支持；`dimension + operator + value` 的整体语义由证据支持；一个 Atom 最多一条正式 Cue；一个 predicate 只含 1–3 个最少且充分的 clause。人物角色、否定、范围、条件、假设和时间语气继续采用保守过滤与人工抽检，不能被误写成纯本地确定性证明。
5. 运行必须支持安全中断、同 run 恢复、防重复和覆盖检查。checkpoint 是最少恢复状态；最终权威产物只由同一 checkpoint 整理而来。并行或第二账号只有在单机实际吞吐不足时才是可选优化，不能成为启动正式生产的前置条件，也不得预建按 worker、package、请求、attempt 或 dimension 划分的分布式目录。**账号只是算力来源，不是论文变量**，因此不要求用第二账号专门生产一批“证明账号同质”的数据（旧设计的 900 条验收集正是为此存在，已取消）。若确实需要第二账号，只允许以下最小做法：两个账号使用完全相同的模型、prompt、Schema、参数和 `run_id`；Atom 按确定顺序静态划分、互不重叠，任何 Atom 只由一个账号处理；另取少量重复 Atom 只用于检查是否存在账号级灾难性差异（例如同一 Atom 在两侧结论完全相反）。不重叠划分与重复样本都记录在同一个 `run_id` 的 checkpoint 中，不另建目录或账本。
6. 完成全量请求后，执行四项闭合检查：10,000 Atom 全覆盖；终态唯一性；accepted Cue 的全量确定性验证（Schema、连续 span、value 直接支持、血缘可解析）；分层人工抽检约 100 条。抽检按 dimension、split 与终态分布分层抽取，重点仍是会在全量中重复出现的语义错误，而不是逐条复检。通过后才生成该 `run_id` 的正式稳定输出；这不是 manifest/SUCCESS 读门，而是确认输出确实完整、可供 Seed 使用的必要数据检查。
7. 抽检发现问题时按错误性质分两种处置，不做夹在中间的第三套流程：
   - **个别错误**：修生产器或验证规则后，只确定性重建受影响的 Atom。重建必须走新 `run_id`，或在同一 `run_id` 下由新记录覆盖该 Atom 的旧终态；不得手改 JSONL，也不得让旧、新两种条件下的结果共存于同一文件。
   - **系统性错误**：改变事实含义的错误在同一样本层内重复出现时，整个 `run_id` 作废，修正后从候选开头整体重跑；不迁移、不拼接、不选择性保留。

完成 50、100、500 或任意部分数量均只是阶段 D 的运行进度，绝不代表 Cue 阶段完成。Seed 接口联调在全量 Cue 闭合前只能使用 synthetic fixture 或明确标记的非正式样例；Seed 正式生产不得读取 checkpoint、局部输出、临时结果或部分 Cue 分片。

阶段 D 的完成条件：10,000 个候选 Atom 均已取得且仅取得一个正式终态；所有 accepted Cue 通过全量确定性验证；分层人工抽检没有发现已知系统性错误；正式输出已由该同一有效 `run_id` 闭合。只有同时满足这些条件，Seed 正式生产才能开始。

#### 阶段 C/D 实施前需要由 T0 处理的合同冲突

以下冲突与未明确事项都在 2026-09-16 实测确认过，不是推测。本方案不把它们假装为已解决，也不因此恢复旧式验收流水线；它们必须由 T0 明确废止、缩短或迁移后，阶段 C 才可在单一规则下实施。

1. **`AGENTS.md` 仍是 `v1.5.0` 完整合同，与“去治理”方向直接冲突。** 它仍规定 T0–T4 文件所有权与正式启动门、SUCCESS 哈希读门、`*.tmp` 原子替换加 SUCCESS 标记的强制流程、“配置/Schema 不可改”的口径，以及“全项目工件与目录节制硬门禁”——后者还要求“新增或修改任何阶段的生产者时，对应测试必须断言正式目录中的文件集合与冻结预算恰好相等”。按本方案，阶段 C 的 checkpoint 目录会随运行存在、正式产物也不按冻结预算枚举，两者无法同时满足。阶段 A 第 4 项的“缩短 `AGENTS.md`”尚未执行。
2. **`AGENTS.md` 已经指定了与本方案不同的 Cue v2 正式布局。** 该文件写明 Cue v2 使用 `cues/v2/formal/` 下的正式分片、一个 manifest 和一个阶段 SUCCESS。本方案只保留同一 `run_id` 的 `cues.checkpoint.jsonl` 与 `cues.jsonl`，不建分片、manifest 或 SUCCESS。目录名与文件集合必须由 T0 选定其一，不能两套并存。
3. **`config/paths.yaml` 仍在声明 SUCCESS 与哈希门。** 它仍列出 `success_markers`（`cue_library`、`seed_candidates`、`frozen_seeds`、`lifelogs`、`decisions`、`final_validation` 等）与 `atomic_write.success_marker_required_fields`（含 `sha256`、`row_count`、`upstream_hashes`），并保留 `artifacts.cue_library_manifest`。这些与“脚本默认不读取 SUCCESS、manifest、旧账本”冲突。
4. **`config/model_registry.yaml` 仍带旧 manifest 合同。** 顶层 `run_manifest_required_fields` 仍要求 `raw_response_path`、`cue_execution_protocol_sha256`、`package_maximum_atoms`、`batch_task_id`、`package_count` 等字段；`cue_v2_extraction.runs_root` 仍是 `../cues/v2/runs`，与第 5 节 C.3 的 `cues/v2/formal/` 目标不一致。CLI 可覆盖 `runs_root` 的路径只有这一处配置，必须一次改定。
5. **两份 Cue v2 Schema 与三种终态不一致，且有测试锁定旧口径。** `cue_v2_inference.schema.json` 的 `non_accepted.disposition` 含 `unsupported`；`cue_v2_artifacts.schema.json` 的 `base.status` 含 `needs_review` 与 `deferred_unsupported`；两者都没有 `excluded_ambiguous` 或 `accepted_cue`。更关键的是 `tests/test_cue_v2_current.py` 用两条断言把 `unsupported` 和 `deferred_unsupported` 固定下来，阶段 C 若不改这个测试就无法落地三种终态。
6. **`05_extract_cues.py` 的 900/120 验收计划仍有可被 import 的接口。** `build_protocol_acceptance_plan`、`preflight_bge_selection_snapshot`、`partition_cue_v2_candidates`、`_cue_v2_calibration_records` 目前只被 `test_qwen_contracts.py` 使用，而该模块带模块级 `legacy_v36` 标记并被默认 `addopts` 排除。删除这些函数必须同时处理那批测试，不能只删函数留下一批必失败或永远不运行的测试。
7. **`05_extract_cues.py` 的当前 CLI 只有预检能力。** 现有参数为 `--legacy-v9`、`--model-registry`、`--source-atoms`、`--candidates`、`--inference-schema`、`--prompt`、`--run-id`、`--limit`、`--dry-run`、`--execute`；本方案 C.2 需要的 `--max-requests`、`--max-cost-cny`、`--timeout-seconds`、`--batch-size`、`--resume`、`--finalize`、`--retry-failed` 都还不存在，`--execute` 目前直接抛 `阶段 B 只允许 Cue v2 只读预检`。阶段 C 必须把真实执行、有限重试、checkpoint、恢复与全量闭合接入这同一入口，不得借此重建旧 V9 的 realtime/package 体系。
8. **`06`/`07`/`11` 的默认输入仍是旧合同。** `06_retrieve_trigger_lures.py` 默认写 `cues/TRIGGER_LURES_SUCCESS.json` 并读 `cue_candidate.schema.json`；`07_generate_seed_candidates.py` 要求上游标记 `artifact_type == "cue_library_manifest"` 并读取 `cue["cue_type"]`；`11_validate_all.py` 多处依赖 `cue_library_manifest`。阶段 C/D 期间它们只能使用 synthetic fixture 或明确标记的非正式样例做接口准备；正式读取接口只能在阶段 D 全量闭合后实施。
9. **`pyproject.toml` 仍描述验收计划测试，且默认套件不覆盖 validator。** 其 `markers` 仍写着 `cue_v2: Cue v2 协议、验证器与验收计划测试`，`addopts` 仍以 `-m 'not legacy_v36'` 整体排除 88 条测试。结果默认 `python -m pytest -q` 只有 13 条通过，且没有一条 validator 或 contract-freeze 测试。阶段 C 需要决定这个 marker 的去留，否则“默认全绿”没有覆盖意义。
10. **未提交状态必须在阶段 C 期间保持原样。** 当前 tag `stage-a-recovery-20260915-01` 已存在；`egopm_bench_v1/cues/v2/source_selection/**`（含 10,000 条正式候选五文件）与本执行方案文档本身都仍是未跟踪文件，Git tag 不能保护它们。任何阶段 C 动作都不得 reset、checkout 或覆盖这些路径。
11. **首批正式确认的运行参数仍未确定。** 实际的预算、`--max-requests`、成本上限、超时、批大小与人工抽检条数由用户在启动 API 前确定；它们是运行安全参数，不是协议验收 campaign，也不构成新的放行门。

### 阶段 E：Seed 接口联调与下游验证

1. 修改 `06_retrieve_trigger_lures.py`，只读取阶段 D 指定的完整正式 Cue `run_id` 和 Source，不读取 checkpoint、局部输出、旧 manifest/SUCCESS/75 分片，也不从多个运行中任意挑选 accepted Cue。启动时必须重新检查完整候选覆盖和最终状态集合；不满足时立即拒绝。
2. 修改 `07_generate_seed_candidates.py`，使用 Cue v2 的 clause predicate 与 `trigger_cue_id`；`trigger_cue_id → trigger_atom_id → trigger_predicate` 必须能精确解析。predicate 改变时，引用它的 Seed 与 Life Log 必须重建；删除对旧 `cue_type`、旧 compact 字段和旧 shard 形状的依赖。
3. 一个 Seed 的 trigger 与全部 lures 必须属于同一 split，防止跨 split 泄漏；至少保留一个同 `source_group_id` 的同上下文 lure，以及一个不同 `source_group_id` 的跨上下文 lure。每个 lure 必须记录至少一个未满足的 trigger predicate clause，不能只以“看起来不相同”作为理由。
4. 最终基准中禁止 `trigger_cue_id`、trigger Atom 和 lure Atom 跨 Seed 复用，避免样本间记忆泄漏与伪重复。开发小样本可为调试暂时放宽该规则，但输出必须显式标为开发样本，不能混入最终基准。
5. 同一 Seed 的正负 Life Log 配对必须共享 trigger、lures 与目标事实观察，只改变一条显式标记的 constructed 生命周期控制事件，使 `remind/silent` 翻转。Source 事实与 constructed 生命周期事件必须在结构中可区分。
6. 模型可见输入不得泄漏 trigger/lure 角色、gold、反事实分支或 oracle 理由；这些只保留在隐藏标注。gold 仍由确定性状态机生成，不由模型决定。
7. Seed、Life Log 与 oracle 的当前脚本沿用同样原则：输出结构由功能与上述科学约束决定，不为了治理生成额外 marker/ledger/manifest。

阶段 E 的完成条件：阶段 D 的完整正式 Cue 可以生成至少数十条 Seed 联调样本，并能编译为 Life Log 和 oracle 决策；Cue 血缘、split/lure 构成、Source/constructed 区分、隐藏标注和 `remind/silent` 配对翻转均正确。这里允许 Seed 仍是小样本，但 Cue 必须已经全部处理完成。

### 阶段 F：归档或删除已隔离的旧 V9/runtime

1. 只在阶段 A 的可恢复点、阶段 B 的 Cue v2 当前路径、阶段 C 的首批正式确认、阶段 D 的完整正式 Cue，以及阶段 E 的 Seed 接口联调均完成后，重新盘点旧 V9 formal、旧入口、旧 prompt/Schema/测试、realtime 波次和根账本。
2. 任何仍被当前代码、测试或文档引用的对象均不得删除；先解除引用，再向用户展示精确路径、文件数、字节数、Git 跟踪状态和恢复方式并取得确认。
3. 已跟踪内容可在确认 Git 恢复点后删除；ignored/未跟踪 realtime 内容必须先整体提交或完成仓库外归档。不得用 Git tag 假装保护 ignored 文件。
4. BGE proof 等非阻断 provenance 只在整体提交或仓库外归档后，交由用户决定是否删除；不作为新 Cue 路径的日常读门。

阶段 F 的完成条件：旧 V9/runtime 是否删除已有用户确认和可恢复路径；当前 `05 → 06 → 07` 不再依赖其任何内容。

### 阶段 G：最后处理 Source 中间文件并收敛文档和测试

1. 只有在阶段 F 后，才重新盘点并由用户决定是否删除可重建的 Source draft、raw segments、缓存和临时文件；这些文件不影响当前代码复杂度，因此不抢在 Cue v2 稳定前处理。保留 `SOURCE_ATOMS_SUCCESS.json`、`srt_inventory.csv` 和 `atom_build_report.json` 作为轻量 provenance 元数据。
2. 根 `README.md` 最终只说明研究目标、当前一条流水线、如何运行和数据边界；后续应另以一份简短 `DESIGN.md` 收敛长期科学约束，包括 Cue 事实边界、split/lure、Life Log 隐藏标注和确定性 oracle。本方案只是迁移期间的执行说明，不应成为唯一长期科学规范；本次不创建或修改这两个文件。
3. 删除或压缩 `CHANGE_REQUESTS.md`、重复状态板、历史 handoff、重复生产指南；重要设计决定最终收敛到简短 README/DESIGN.md 或代码注释，不保留第二份状态真相。
4. 测试只保留三类：Source 解析/事实边界、Cue/Seed/Life Log 的核心正确性、最终状态机与防泄漏检查。
5. 删除 legacy marker；默认测试必须是当前路径测试。历史代码不进入当前 CI 或本地默认命令。

阶段 G 的完成条件：Source 可重建中间文件是否删除已有用户确认；`python -m pytest -q` 只反映当前系统；README、配置、代码和测试只描述同一条路径。

## 6. 最小质量检查替代方案

为避免“完全无检查”导致研究结果不可用，采用以下低摩擦替代：

| 目标 | 保留的检查 | 明确取消的检查 |
| --- | --- | --- |
| Source 事实 | 字段存在、时间范围、split 基本一致，`event_timestamp` 只用于定位/排序 | SUCCESS 哈希读门、冻结版本比对 |
| Cue 事实 | Cue 只能 Source-grounded；同字段连续 span、value 直接支持、角色/否定/时间语气检查；单个 `run_id` 隔离 | 每条 run 的 prompt/Schema/worker 哈希链 |
| Seed | 可精确解析的 Cue 血缘、同 split、同/跨 `source_group_id` lure、每个 lure 有未满足 clause；最终基准不跨 Seed 复用 | cue manifest、75 shard、全局 worker ledger |
| Life Log | constructed 标记、Source/constructed 可区分、隐藏角色/gold/oracle 信息、状态转移、成对分支只改变控制状态 | 分布快照四文件、阶段 SUCCESS |
| 发布 | Git tag（仅限已跟踪内容）、行数、生成命令、模型名，以及少量非阻断 provenance 元数据 | 多层 manifest/SUCCESS/receipt 链；SHA256 仅在外部发布方要求时可选 |

开发态与归档/发布态必须明确分离：开发态可直接读取当前输入并改代码，不使用 SUCCESS、manifest 或 hash 作为运行阻断；归档/发布态只保留能说明 Source 行数、来源与身份的少量 provenance 元数据，以及经用户选择保留的 BGE 整体工件，不再建立多层治理链。Git tag 只覆盖已跟踪内容，未跟踪或 ignored 数据必须通过整体提交或仓库外归档获得恢复路径。

## 7. Codex 的执行顺序与禁止事项

Codex 必须按以下顺序工作；它与第 3.5 节的归档/删除批次和第 5 节的阶段 A–G 完全一致：

1. 建立可恢复点并做引用盘点，不删除（已完成）：保护未提交改动，确认已跟踪/ignored 内容的恢复方式，列出仍被当前代码、测试或文档引用的对象。恢复点 tag 为 `stage-a-recovery-20260915-01`。
2. 重写当前 Cue v2 的配置、CLI 和测试，解除对旧路径的默认依赖，但不物理删除旧对象（已完成）：默认入口现为只读预检。
3. 完成正式 Cue 生产器并进行首批约 50 条正式确认；真实调用必须显式使用 `--execute`、有限 `--limit`、`--max-requests` 和单请求超时，并输出 token/费用汇总。首批结果保留在正式 run 中，但不得进入 Seed。本步同时删除或降级 900/120 验收计划设计。
4. 使用第 3 步相同的正式 `run_id` 处理完整候选池；确认候选集合完全覆盖、所有行均为最终状态，并且没有生成 Seed。
5. 让 06/07 只消费第 4 步的完整正式 Cue，完成 Seed 接口联调。
6. 重新盘点并经用户确认后，归档或删除已经隔离的旧 V9/runtime；已追踪历史使用 Git 恢复点，ignored 大文件先整体提交或仓库外归档。
7. 最后才在重新盘点和用户确认后考虑删除 Source draft、raw segments、缓存和临时文件；Source 中间文件不能抢在 Cue v2 稳定前清理。

任何实质删除前，Codex 必须展示精确路径、文件数、总字节数、Git 跟踪状态、当前引用、恢复方式和拟执行动作；任何仍被当前代码、测试或文档引用的对象不得删除。每一阶段只运行与其直接相关的检查并报告实际代码进展，不新增状态板、CR、SUCCESS 或 SHA 文件。

禁止事项：

- 不要重建 Source，除非已确认 Source 本身有 bug。
- 不要为了保留历史再复制一份旧目录到仓库内。
- 不要保留“兼容旧路径”的 fallback；兼容层是当前屎山继续生长的主要来源。
- 不要将历史测试标记为 optional 却仍让它们默认运行。
- 不要为了满足旧合同写新的 manifest、ledger、receipt、SUCCESS、distribution 或 hash 文件。
- 不要删除用户未确认的大型数据、未提交改动或唯一的可恢复副本。
- 不要把“去治理”理解为允许无限量 API 调用：缺少 `--execute`、有限 `--limit`、请求上限或超时的真实运行必须拒绝；成本/运行安全不是审批负担。

## 8. 完成定义

本次优化完成时，应满足以下可观察结果：

- 当前代码不再依赖 V9 Cue、V3.6 realtime、八波运行、75 分片、历史 worker、CR 或 SUCCESS；开发态不把 SUCCESS、manifest 或 hash 作为运行阻断。
- 阶段 C 的首批正式确认结果保留在同一正式 `run_id` 中；阶段 D 已用该 `run_id` 完成全部候选的正式 Cue 生产，Seed 从未读取任何局部输出。
- `05 → 06 → 07` 能在不读取旧 Cue 工件的情况下，让 06/07 只消费完整正式 Cue 并完成 Seed 接口联调。
- Cue 运行以 `run_id` 隔离：checkpoint 可恢复中断，finalize 原子生成单一运行的 `cues.jsonl`；下游只消费所选 `run_id` 的 accepted Cue，predicate 变更会重建下游引用。
- 真实 API 运行必须显式使用 `--execute`、有限 `--limit`、`--max-requests` 和单请求超时；有限重试、运行期 token/费用汇总、成本上限与 checkpoint 恢复可用，但不产生旧式账本或治理读门。
- Cue 始终是 Source-grounded；最终 Seed 保持同 split、同/跨 `source_group_id` lure 与不满足 clause 约束，最终基准不跨 Seed 复用 Atom；Life Log 保持 Source/constructed 区分、隐藏标注和确定性 gold 的正负配对设计。
- `python -m pytest -q` 通过，且测试没有 legacy 分支。
- 旧 realtime package/ledger 目录和 V9 formal 大文件只在可恢复点、Cue 首批正式确认、完整正式 Cue 生产和 Seed 接口联调均完成后，才可逐批重新盘点并由用户确认是否删除；已跟踪历史可由 Git tag 恢复，未跟踪或 ignored 内容必须先整体提交或仓库外归档，Git tag 不能单独保护它们。
- Source draft、raw segments、缓存和临时文件只在旧 V9/runtime 处理之后才考虑删除；任何仍被当前代码、测试或文档引用的对象不得删除。
- `SOURCE_ATOMS_SUCCESS.json`、`srt_inventory.csv` 和 `atom_build_report.json` 保留为非阻断 provenance 元数据；BGE 五文件按整体提交或仓库外归档处理后，再由用户决定是否删除非候选元数据。
- `AGENTS.md` 不再禁止普通配置/Schema/代码修改，也不再要求角色审批或哈希解锁。**当前尚未做到**：工作区中的 `AGENTS.md` 仍是 `v1.5.0` 完整合同，缩短它属于阶段 C 之前的合同冲突处理项，见第 5 节末第 1、2 项。
- 迁移完成后，长期科学约束收敛至 `README.md` 加简短 `DESIGN.md`；本执行方案仅作为迁移说明，不是唯一长期规范。
- 未来 Codex 的主要工作量回到 Cue、Seed、Life Log、oracle 和评测代码本身，而不是解除旧协议限制。

## 9. 下一次执行的首个任务

阶段 A 与阶段 B 已完成：恢复点 tag `stage-a-recovery-20260915-01` 已存在，`05_extract_cues.py` 的默认 CLI 已是 Cue v2 只读预检，默认不读 realtime、manifest、SUCCESS 或 proof。因此下一次实施不再重复 A/B，而是直接进入阶段 C，且第一阶段 C 动作是处理第 5 节末的 11 项合同冲突，其中至少三项必须先于任何 API 调用完成：

1. 由 T0 缩短 `AGENTS.md`，并选定 Cue v2 的正式文件集合与目录名（`cues/v2/formal/` 还是 `cues/v2/runs`），使 `AGENTS.md` 与本方案只剩一套规则。
2. 把 `config/model_registry.yaml` 的 `runs_root`、`config/paths.yaml` 的 `success_markers` 与两份 Cue v2 Schema 的终态口径对齐到第 5 节 C.3/C.4，并同步去掉 `tests/test_cue_v2_current.py` 中锁定 `unsupported`/`deferred_unsupported` 的断言。
3. 从 `05_extract_cues.py` 的当前路径移除 900/120 验收计划函数及其 legacy 测试，补上真实 `--execute`、`--max-requests`、`--max-cost-cny`、`--timeout-seconds`、`--batch-size`、`--resume`、`--finalize`、`--retry-failed` 与单 run checkpoint。

完成这三项后再进行首批约 50 条正式确认，随后用同一个 `run_id` 完成全部 10,000 条；只有全量闭合后，阶段 E 才让 06/07 读取完整正式 Cue，阶段 F/G 才按重新盘点和用户确认进行不可逆清理。除目标 Markdown 外，本次不修改任何代码、配置、Schema、测试、`AGENTS.md`、状态板或其他文件。
