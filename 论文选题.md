# 论文选题

> 2026-09-09 Cue/Seed v2 修订：正式目标调整为 480 个独立 Seed、900–1,400 个候选 Seed 和 2,880 条六路 Life Log。旧 V9 Cue 仅作审计；新 Cue v2 采用 Source→WSL BGE-M3 候选筛选→三个百炼账号同一 Flash 协议抽取→clause-level 语义门。若本文后续历史段落与本修订冲突，以本修订和 `egopm_bench_v1/coordination/CUE_SEED_V2_PLAN.md` 为准。

## 一、题目与研究定位

> **中文题目：**
>
> 面向流式第一视角助手的预算约束前瞻记忆：基准、决策证据与生命周期感知记忆
>
> **英文题目：**
>
> **EgoPM: Benchmarking Budgeted Prospective Memory for Streaming Egocentric Assistance**

所属方向：多模态长期记忆、第一视角视频理解、前瞻记忆、主动式智能助手。

### 1. 研究目标

本文主要提出 **EgoPM-Bench**，用于评价智能助手在单遍、不可回看的第一视角生活观察流中，能否仅依靠容量受限的持久记忆维护跨时前瞻意图，并在没有用户查询的情况下自主决定何时提醒、提醒什么或保持沉默。v1 采用 **video-referenced, text-first** 的构建方式：最终原子仍对应 EgoLife 视频时间段，但第一版只消费同时间段的 Transcript / Dense Caption 与媒体索引，不要求预先下载或拼接原始视频；视觉输入作为后续扩展轨道。

本文同时提出 **CuePM（Cue-Aware Prospective Memory for Proactive Assistance）**，作为面向 EgoPM-Bench 的生命周期感知预算记忆方法。论文整体采用 **Benchmark-first，method-supported** 的定位：Benchmark 是主要贡献，CuePM 用于验证该任务是否需要面向前瞻决策的专门记忆机制。

### 2. 一句话概括

> EgoPM-Bench 研究跨小时或跨天的用户前瞻意图，如何在第一视角多模态流、历史不可回看和持久记忆预算受限的条件下被正确形成、修改、保持、触发和终止；CuePM 则学习保留未来提醒或沉默决策所需的意图状态与历史证据。

### 3. 主要贡献

#### 贡献一：预算约束的流式第一视角前瞻辅助基准

提出 EgoPM-Bench，将前瞻记忆评价扩展到来源锚定的第一视角长时生活流。系统不知道候选决策点，需要在统一持久记忆预算下持续维护用户意图，并在每个时间步自主输出提醒集合或保持沉默。v1 的公开观察为与视频时间段绑定的官方文本，后续视觉扩展可按相同 atom 引用延迟加载对应视频。

本任务的核心组合是：

- 跨小时或跨天的用户前瞻意图；
- 第一视角视频、语音、活动和时间等多模态线索；
- 历史原始观察不可回看；
- 文本或结构化状态形式的持久记忆受到显式容量约束；
- 意图具有创建、修改、提醒、完成、取消和过期等生命周期；
- 正确沉默与及时提醒具有同等重要性。

本文不将“主动提醒”或“逐时刻 `remind/silent`”单独宣称为首次提出。区别于文本模拟的前瞻记忆基准和面向短程程序错误的第一视角主动辅助，EgoPM-Bench 重点研究跨时用户意图在受限持久记忆下的维护与触发。

#### 贡献二：面向前瞻决策的生命周期与证据标注

为每条前瞻意图标注意图形成、触发条件、修改、完成、取消、过期和历史提醒记录；为有效提醒点以及与特定意图相关的困难沉默点标注一个或多个**决策支持证据集合（decision-support evidence sets）**。

证据价值被定义为证据与目标意图、未来决策之间的条件关系，而不是证据自身的静态属性。标注允许多条证据相互替代或共同构成充分条件，避免假设每个决策只有唯一的“最小必要证据”。

#### 贡献三：生命周期感知的预算记忆方法 CuePM

CuePM 将持久记忆划分为前瞻状态账本与决策证据缓存。状态账本维护行动、触发条件、有效期、最新状态和提醒历史；证据缓存保留可追溯的意图形成与状态变化证据。

CuePM 使用训练集中可见的未来决策证据关系作为监督，学习一个在线证据价值预测器；测试时预测器只能使用当前及历史信息，并在固定 token 预算内进行保留、合并和删除。方法目标不是概括过去，而是尽可能保留未来正确提醒或沉默所需的信息。

---

## 二、任务定义

### 1. 前瞻意图（Prospective Intention）

前瞻意图是在时间 $t$ 形成、但需要在未来某个时间或条件满足时执行的行动计划。

每条意图至少包含：

- `intention_id`：意图唯一编号；
- `source_evidence`：形成该意图的视频、语音或文本证据；
- `action_content`：未来需要完成的行动；
- `trigger_condition`：时间、事件、地点、人物、物体或活动等触发条件；
- `validity_interval`：生效时间和过期时间；
- `status`：`active / completed / cancelled / expired`；
- `update_history`：创建、修改、延期、完成、取消和过期记录；
- `reminder_history`：历史提醒时间、次数和内容。

`remind/silent` 是决策点的动作标签，不是意图自身的字段；`creation/revision/completion/cancellation/expiration` 是状态更新事件，也不应被限制为只在 `active` 状态下存在。

### 2. 意图生命周期

```text
意图形成
→ active
→ revision / reschedule：更新后继续 active
→ remind：继续 active，并写入 reminder_history
→ observed_execution：completed
→ cancellation：cancelled
→ 超出有效期：expired
```

系统发出提醒并不表示任务已经完成。只有观察到用户执行任务、用户明确确认完成，或数据标注提供等价完成证据时，意图才能转为 `completed`。

### 3. 前瞻记忆（Prospective Memory）

前瞻记忆是系统编码、保持、更新并在适当条件下提取前瞻意图，从而在未来正确时机促进意图执行的能力。

任务包含：

- **事件触发型**：下次见到某人、到达某地或看见某物时提醒；
- **时间触发型**：在指定时间或截止日期前提醒；
- **活动触发型**：开始或结束某项活动时提醒下一步操作。

### 4. 在线决策形式化

系统按顺序接收多模态观察流 $O_1,O_2,\ldots,O_T$。在时刻 $t$，系统只能访问当前观察 $O_t$ 和上一步持久记忆 $M_{t-1}$：

$$
M_t=\operatorname{Consolidate}(O_t,M_{t-1};B),
\qquad \operatorname{Size}(M_t)\le B.
$$

记忆更新后，系统输出：

$$
A_t=\operatorname{Trigger}(O_t,M_t),
\qquad
A_t\subseteq\{\operatorname{remind}(i)\mid i\in\mathcal I_t\}.
$$

当 $A_t=\varnothing$ 时表示 `silent`。使用集合形式是为了允许多个意图在同一时刻到期；若实验中限制每个时间步最多提醒一个意图，则必须额外定义优先级策略。

系统在时刻 $t$ 不能访问 $O_{t+1:T}$，也不能重新访问已经处理的原始历史视频。候选决策点和有效提醒窗口只对评测程序可见，不提供给模型。

---

## 三、EgoPM-Bench

### 1. 数据来源与可用模态

EgoPM-Bench 计划主要基于 EgoLife 构建。EgoLife 包含六名参与者一周的长时间第一视角生活记录，以及视频、音频、语音转写、视觉—音频描述和时间信息，适合寻找跨片段、跨小时甚至跨天的意图形成与触发关系。

v1 使用的输入包括：

- 完整保存的 Transcript / Dense Caption SRT 原始路径；
- 对应第一视角视频的路径或 UID、片段起止位置与参与者/日期信息；
- 带说话人信息的语音转写、视觉—音频描述与绝对/相对时间戳；
- 显式标为 constructed 的意图、时钟、修改、取消、完成和过期观察。

视频是最终原子所引用的媒体来源，但 v1 不把 MP4 复制进 benchmark，也不要求生成阶段读取视频内容。每个 atom 必须同时保留 `source_srt_path` 与 `source_video_path`：前者保证文本和时间戳可审计，后者保证未来能按需恢复实际视频。只有在视觉扩展中，才根据 manifest 延迟下载或读取对应视频片段。

EgoLife 原有的 EgoLifeQA、TaskMaster 和支持证据时间戳可以用于候选发现与基线构建，但 EgoPM-Bench 需要新增前瞻意图生命周期、在线提醒窗口、困难沉默点和预算约束协议，不能直接把原有问答实例视为前瞻记忆实例。

### 2. 样本层次

#### Prospective Memory Episode

一条完整前瞻意图生命周期，包括形成、保持、修改、触发、提醒、完成、取消或过期等事件。

#### Triggering Decision Instance

Episode 中的一个提醒或沉默决策点。一条 Episode 可以产生多个决策实例，例如：

- 意图形成后尚未满足触发条件：`silent`；
- 出现相似但错误的线索：`silent`；
- 满足有效触发条件：`remind`；
- 已完成或取消后再次出现线索：`silent`；
- 冷却期内再次出现线索：根据预定义提醒策略判断 `silent` 或再次提醒。

### 3. 数据类型

- **Source Video Atom**：来自 EgoLife Transcript / Dense Caption 的真实可观察事件，绑定 SRT 与视频时间段；
- **Grounded Constructed Intention**：以真实 trigger atom 为锚点，反向构造并注入更早虚拟时间的明确前瞻意图；
- **Constructed Lifecycle Observation**：为规则运行显式加入的修改、取消、完成、过期、时钟或日历观察；
- **Counterfactual Lifelog**：保持当前 trigger atom 不变，只改变历史中的意图存在性、状态或提醒历史，形成动作翻转的成对生活流。

EgoPM-Bench v1 不把 Natural Intention 作为正式数据类型，也不声称构造的生命周期自然发生于 EgoLife。所有事件必须保留 `source_video_atom / constructed_intention / constructed_observation / derived_label` 的来源标签。主体 benchmark 为 Grounded Constructed 与 Counterfactual 数据；原始视频输入留作独立视觉扩展。

### 4. 原始事件标注

```json
{
  "world_event_id": "world_021",
  "episode_id": "pm_001",
  "intention_id": "i_001",
  "event_id": "event_001",
  "participant_id": "p01",
  "video_uid": "xxx",
  "source_srt_path": "EgoLifeCap/DenseCaption/A1_JAKE/DAY1/example.srt",
  "source_video_path": "data/EgoLife/A1_JAKE/DAY1/example.mp4",
  "source_start_sec": 120.5,
  "source_end_sec": 145.2,
  "virtual_time": "D01 11:20",
  "transcript": "...",
  "dense_caption": "...",
  "provenance": "source_video_atom",
  "event_role": "trigger_context"
}
```

`source_srt_path` 与 `source_video_path` 都要保存，二者用途不同：SRT 路径用于复现当前文本输入及其时间戳，MP4 路径用于未来视觉轨道定位媒体。构造意图和构造生命周期观察没有来源 MP4，可以将媒体字段设为 `null`，但必须具有合法 `rule_id` 与生成记录。

`event_role` 取值包括：

- `intention_creation`：意图形成；
- `intention_revision`：意图修改或延期；
- `intention_cancelled`：意图取消；
- `intention_expired`：意图过期；
- `trigger_context`：当前视觉或语言情境满足触发线索；
- `non_trigger_context`：看起来相似但不应提醒的情境；
- `intention_execution`：用户执行意图；
- `intention_completed`：可确认任务已经完成；
- `reminder_delivered`：系统或外部人员已经提醒过。

### 5. 决策点与证据标注

每个决策实例至少包含：

```json
{
  "decision_id": "d_017",
  "episode_id": "pm_001",
  "target_intention_id": "i_001",
  "decision_time": 6120.0,
  "valid_window": [6060.0, 6240.0],
  "gold_action": "silent",
  "decision_type": "post_completion_lure",
  "counterfactual_pair_id": "cf_004",
  "evidence": [
    {
      "event_id": "event_creation",
      "role": "intention_definition",
      "necessity": "supporting",
      "support_group_id": "g1"
    },
    {
      "event_id": "event_completed",
      "role": "state_invalidation",
      "necessity": "essential",
      "support_group_id": "g1"
    }
  ]
}
```

`necessity` 建议使用：

- `essential`：在当前支持集合中不可缺少；
- `supporting`：有助于确定正确决策，但可能存在等价替代证据；
- `redundant`：与其他证据语义重复；
- `distractor`：与目标决策无关或具有迷惑性。

`support_group_id` 表示一个能够支持正确决策的证据组合；同一决策可以具有多组等价的支持集合。评价证据保留时应比较语义事实或状态槽是否被保留，而不是要求模型逐字保存原始片段。

只对与特定意图相关的困难沉默点标注决策证据，例如取消后、完成后、过期后或提醒冷却期内再次遇到线索。普通无意图时刻的 `silent` 可以不标证据，避免产生大量没有明确目标意图的负样本。

### 6. 证据删除与对比验证

证据删除或替换实验用于评价模型是否依赖已标注证据，而不直接作为“客观证明证据因果必要性”的唯一依据。

评测包括：

- **Comprehensiveness**：删除支持证据后，正确动作概率下降多少；
- **Sufficiency**：仅保留某个支持证据集合时，模型能否保持正确决策；
- **Counterfactual Consistency**：当前场景相同但历史意图或状态改变时，模型是否相应改变提醒决策；
- **Semantic Evidence Coverage**：预算记忆是否保留完成决策所需的语义事实或状态槽。

### 7. 数据构建流程

v1 采用 **decision-first source-grounded compiler**，不先生成 MBTI 人物或完整自然日程，也不要求从 EgoLife 中筛出天然完整 Episode。完整生产流程如下：

1. 全量解析 Transcript / Dense Caption SRT，按参与者、日期、源文件和时间段建立可追溯的视频事件原子；
2. 以宽松标准保留可观察事件，只删除空文本、乱码、无有效时间、不可理解噪声和相邻完全重复项；
3. 在同一参与者/日期/时间范围内对 Transcript 与 Dense Caption 进行时间对齐，生成统一 `source_event_atoms.jsonl`；
4. 先按来源组冻结 train/dev/test，严禁后续同一源 atom 或近重复文本跨 split；
5. 在 WSL2 中用固定 BGE-M3 对全量 Source 做 dense+sparse+多样性筛选，冻结 8,000–12,000 个候选 Atom；
6. 三个阿里云账号使用同一个 `qwen3.7-flash` Cue v2 协议处理静态不重叠分区，生成约 3,000–5,000 条具有 clause-level 原字段证据的高质量 Cue；
7. 从 Cue v2 生成 900–1,400 个候选 Reminder Seed。每个 Seed 必须具有一个真实 trigger atom、至少两个真实 lure/distractor atom、可执行 trigger predicate、可解释的 terminal/silent 逻辑，且所有真实 atom 来自同一 split；
8. 对全部候选执行程序验证、低成本模型意见和人工终审，冻结 480 个基础任务；若不足 480 个，只能扩大候选池，不能降低质量门；
9. 为每个通过审计的基础任务构造 positive 与 matched-negative 两种历史，保持当前 trigger observation 相同，仅改变 `never_created/completed/cancelled/expired/already_reminded` 等历史状态；
10. 为每个反事实分支生成 short/medium/long 三个匹配难度版本，只改变干扰量、并行意图数、虚拟跨度与 token 负载；
11. 使用 oracle 状态机在每个事件到达后生成 `remind(intent_id)` 或 `silent`，LLM 不直接写 gold；
12. 输出 manifest、Episode spec、Decision Instance、Evidence Set、反事实配对、分布报告和构建审计记录；
13. 对全部正式数据执行 Schema、来源追溯、状态迁移、答案泄漏、split 泄漏、长度匹配与可解性检查。

Cue v2 不再使用顶层 `cue_type`。人物、地点、物体、活动、状态变化和显式时间作为 predicate clause dimension 报告；Cue 只允许 `all_of`，虚拟时间和跨 Atom 条件留在 Seed/Rule。覆盖目标用于发现缺口而不是强迫类别平衡；最终分布以通过审计的 Cue 和 Seed 为准，并报告候选数、接受率、拒绝原因和跨分片漂移。

### 8. 标注质量控制

- 全部基础 Reminder Seed 必须人工审计；批量生成的短/中/长与反事实派生版本执行全量机器检查，并按触发类型、场景、难度和负例类型分层抽取至少 10% 人工复核；
- 对意图类型、状态、动作和证据角色报告分类一致性；
- 对提醒窗口报告时间区间重叠或边界偏差；
- 保存候选被接受、修改和拒绝的原因；
- 对 Source Atom、Constructed、Counterfactual 与后续 Visual Extension 分别统计规模和质量；
- 人工确认测试集中的当前观察本身不能直接泄露历史意图状态。

### 9. 数据划分与泄漏控制

采用训练、验证、测试划分。具体比例在全量 Source Video Atom 建库、按来源组去重并统计各组规模后一次性冻结，不依赖试验性小数据，也不按生成结果反复调整。划分必须满足：

- 同一 `intention_id` 的全部事件和决策点进入同一划分；
- 同一共享现实事件的所有同步第一视角和第三视角内容按 `world_event_id` 整体分组；
- 防止同一对话的不同参与者视角、重复字幕、相邻片段和同一任务跨集合泄漏；
- 优先按日期、场景组合和世界事件分组，而不是仅按参与者划分；
- 测试集包含训练集中未出现的意图—线索组合；
- 同一基础任务的 positive/negative、short/medium/long 以及所有共享 trigger/lure atom 必须作为一个 `family_id` 整体进入同一 split；
- 主结果报告 Grounded Constructed 与 Counterfactual 数据，后续 Visual Extension 独立报告。

由于 EgoLife 只有六名参与者并共享同一周环境，不能仅凭人物不重叠划分就宣称具有广泛跨用户泛化能力。

### 10. 生成模型、审计模型与检索模型的职责

数据构建采用“阶段内一个固定生产模型、阶段间允许不同模型”的政策。Cue v2 的三个 API worker 使用同一个 Qwen Flash 协议；Seed、审计和 Life Log 可分别选用不同中国厂商模型。模型永远不生成金标：

| 环节 | 默认模型/方法 | 说明 |
| --- | --- | --- |
| SRT 解析、时间对齐、去重、split | 确定性 Python 脚本 | 不使用大模型 |
| Source→Cue 候选 Atom 筛选 | WSL2 本地固定 `BAAI/bge-m3` | dense+sparse+RRF+语义多样性；单 GPU 单进程 |
| Cue v2 抽取 | 三个阿里云账号统一使用 `qwen3.7-flash` | 静态不重叠分区；完整词 Schema；clause-level evidence；独立账本 |
| Reminder Seed、trigger predicate、lure 约束和生命周期候选 | 生产前另行冻结；首选候选为 `qwen3.7-plus-2026-05-26` | 900–1,400 个候选，阶段内只用一个模型 |
| Cue/Seed 模型审计 | Plus 低成本主审；DeepSeek 仅少量争议第三意见 | 不进行昂贵模型全量复核；人工审计全部候选 Seed |
| Life Log 语言生成 | 生产前在中国厂商模型中专项选择 | 同一 Life Log campaign 内不得混用模型 |
| `remind/silent`、生命周期状态与 evidence gold | 冻结 oracle 状态机 | 禁止由千问或任何生成式模型直接决定 |

不用 Max。Flash、Plus 或其他中国厂商模型是否适合某个后续阶段，必须在该阶段具有代表性的冻结校准集上比较结构成功率、语义精确率、漏检、成本和吞吐，再冻结一个生产模型。DeepSeek 因费用只处理少量独立审计或争议样本，不承担全量主审。人工审计、程序不变量和反事实配对检查不能省略，也不能把任何模型的同意当作客观金标。

每个 campaign/shard 必须记录精确模型 ID、地域、端点、思考设置、推理强度、采样参数、prompt hash、Schema hash、请求时间范围、输入 Atom 范围、解析结果、验证失败和重试次数；这些元数据不在每条 Cue 重复。正式生产禁止保存原始模型响应。API 密钥只从每个 worker 的环境变量读取，不写入论文目录或日志，也不得打印。

---

## 四、评测协议

### 1. 主轨道：Budgeted Online Track

1. 系统按时间顺序处理完整观察流；
2. v1 处理与视频 atom 绑定的官方事件文本；已处理的原始历史观察不能重新访问；
3. 系统只能使用当前观察和持久化记忆；
4. 持久化记忆不能超过预算 $B$；
5. 系统不能访问当前时刻之后的未来信息；
6. 模型不知道评测候选点，在每个规定时间步自主输出提醒集合或沉默。

处理片段长度、决策频率、提醒冷却时间和最大重复提醒次数，在全量 atom 与 Seed 统计完成后的“协议冻结门”一次性确定，并对所有方法保持一致；冻结后若修改，必须提升协议版本并重新生成全部受影响数据。

### 2. 记忆预算协议

主轨道只允许将持久记忆保存为规范化文本或结构化 JSON，并使用统一指定 tokenizer 计算 token 数。

计入预算：

- 历史事件摘要；
- 意图账本；
- 状态更新；
- 提醒历史；
- 其他跨时间步保存的文本信息。

不计入预算：

- 模型冻结参数；
- 当前时间步的原始观察；
- 当前时间步临时计算且不会跨步保存的中间表示。

主轨道不允许额外持久化、但无法按统一 token 计量的向量索引或隐藏状态。若允许开放式外部记忆系统，应另设 Open-System Track，并使用字节数、向量数量和计算成本等单独指标。

预算档位初步考虑 256、512、1,024 和 2,048 tokens，最终数值根据全部正式数据中意图数量、状态账本长度和证据长度分布确定，并在主实验前冻结。

### 3. 诊断设置与视觉扩展

| 当前输入 | 历史访问 | v1 状态 | 目的 |
| --- | --- | --- |
| 官方事件文本 | 完整历史 | 必做 | 触发与状态推理上界 |
| 官方事件文本 | 预算记忆 | 主轨道 | 隔离记忆管理能力 |
| 原始视频/多模态 atom | 完整历史 | 后续扩展 | 隔离多模态感知误差 |
| 原始视频/多模态 atom | 预算记忆 | 后续扩展 | 端到端视觉主动提醒 |

v1 的论文主结论必须能由前两行独立成立；后两行只有在按 manifest 恢复原视频并完成视觉实验后才报告，不能在仅有文本结果时声称已完成多模态端到端评价。

### 4. 主要指标

#### Event-level Trigger F1

每个意图的每个有效提醒窗口最多匹配一个正确预测：

- 窗口内第一次正确提醒：TP；
- 窗口内没有提醒：FN；
- 无有效意图、意图无效或窗口外提醒：FP；
- 同一窗口中的额外重复提醒：额外 FP，并同时计入重复提醒指标。

若同一时刻存在多个到期意图，应对预测提醒集合与 gold 意图集合进行一对一匹配。

#### False Activations per Hour

每小时错误提醒次数，用于衡量系统对用户的干扰。

#### Trigger Delay

首次正确提醒时间与有效提醒窗口起始时间的差值，同时报告均值、中位数和高分位数，避免少量极端延迟掩盖总体表现。

#### Budgeted Performance AUC

在多个存储预算下测量 Trigger F1，评价性能—存储权衡。需要固定预算横轴和归一化方式，并同时展示每个预算点的原始结果。

### 5. 辅助指标

- **Missed Intention Rate**：有效意图的遗漏比例；
- **Duplicate Reminder Rate**：同一意图的重复提醒比例；
- **State Macro F1**：`active/completed/cancelled/expired` 的宏平均 F1；
- **Semantic Evidence Coverage**：所需语义事实或状态槽的保留率；
- **Counterfactual Consistency**：成对历史发生变化时动作是否正确变化；
- **Reminder Content Score**：提醒内容中的行动、对象和关键约束是否正确；
- **Memory Constraint Violation Rate**：方法超过预算的时间步比例。

---

## 五、方法：CuePM

### 1. 方法概览

CuePM 包含五个模块：

```text
多模态事件编码
→ 前瞻意图与状态更新识别
→ 前瞻状态账本更新
→ 决策证据价值预测与预算巩固
→ 主动提醒或沉默
```

### 2. 多模态事件编码

v1 直接读取与视频时间段绑定的 Transcript / Dense Caption、虚拟时间和构造观察，得到规范化事件表示，避免将感知误差与记忆管理误差混在第一版结论中。后续视觉扩展按 atom 中的 `source_video_path + source_start_sec + source_end_sec` 读取原始媒体，再使用统一 VLM 编码相同事件；文本轨道与视觉轨道共享 Episode、状态机和评测器。

### 3. 前瞻状态账本

对每个已识别意图维护：

- 行动内容；
- 触发条件；
- 有效期；
- 当前状态；
- 最新状态更新；
- 历史提醒记录。

账本中的核心状态槽采用约束式保留和更新。新的完成、取消或修改证据应更新旧状态，而不是简单与过时的 `active` 状态并存。

### 4. 决策证据缓存

证据缓存保存意图形成、修改、完成、取消、提醒和触发背景等可追溯事件。普通场景细节和重复描述可以被合并或删除。

证据重要性不是固定的 $\operatorname{Importance}(e)$，而是证据、当前意图状态与潜在未来决策之间的关系。训练时可以根据未来决策标注构造监督目标，例如：

$$
y_t(e)=\max_{d>t} w(d-t)\,r_{e,d},
$$

其中 $r_{e,d}$ 表示证据 $e$ 是否属于未来决策 $d$ 的某个支持集合，$w(\cdot)$ 是可选的时间折扣函数。

在线测试时只能使用：

$$
\hat v_t(e)=g_\theta(e,S_t,M_{t-1}),
$$

其中 $S_t$ 和 $M_{t-1}$ 只包含截至时刻 $t$ 可获得的信息。真实未来决策 $d$、未来状态变化和未来错误不能作为测试输入。

### 5. 预算记忆巩固

当记忆超过预算时：

1. 先更新并规范化前瞻状态账本；
2. 合并同一意图的语义重复证据；
3. 对证据缓存计算预测决策价值与 token 成本；
4. 在保证核心状态槽完整的约束下选择保留项；
5. 删除低预测价值、冗余或已被最新状态覆盖的信息；
6. 验证序列化后的总 token 数不超过 $B$。

“价值/token”只用于在线预测值和证据缓存选择，不直接使用删除真实证据后观察到的未来错误变化，以避免测试时未来信息泄漏。

### 6. 主动触发

对于每条 `active` 意图，CuePM 联合判断：

```text
触发线索满足
+ 当前时间在有效窗口内
+ 意图状态为 active
+ 符合提醒冷却与重复策略
→ remind(i)
```

否则不将该意图加入提醒集合。完成、取消、过期以及处于冷却期的意图是困难沉默决策的主要来源。

### 7. 训练目标

CuePM 联合学习提醒决策、状态跟踪和决策证据预测：

$$
L=L_{\mathrm{trigger}}
+\lambda_1L_{\mathrm{state}}
+\lambda_2L_{\mathrm{evidence}}.
$$

- $L_{\mathrm{trigger}}$：提醒集合或意图级 `remind/silent` 损失；
- $L_{\mathrm{state}}$：意图状态和状态更新损失；
- $L_{\mathrm{evidence}}$：证据角色、支持关系或在线价值预测损失。

训练时需要处理提醒/沉默和必要/非必要证据的类别不平衡，并通过消融实验分别验证三个目标的贡献。

---

## 六、相关工作与创新边界

| 工作方向 | 已覆盖内容 | 与 EgoPM-Bench 的主要差异 |
| --- | --- | --- |
| [PM-Bench](https://arxiv.org/abs/2607.12385) | 文本化七天模拟、每步前瞻动作、动态更新、主动状态监测 | 不处理真实第一视角多模态感知，也不以统一持久记忆预算为主轴 |
| [TriggerBench](https://arxiv.org/abs/2606.23459) | 无查询主动提醒、负对照、认知负载与误提醒 | 主要是文本/对话场景，不研究单遍第一视角流和显式持久记忆预算 |
| [Plan, Watch, Recover / Pro²Bench](https://arxiv.org/abs/2606.04970) | 流式第一视角视频、逐决策点 `interrupt/silent`、程序错误恢复 | 主要面向短程程序执行与恢复，不研究跨小时/天用户意图的形成、修改和终止 |
| [EgoLife / EgoLifeQA](https://arxiv.org/abs/2503.03803) | 超长第一视角数据、长时问答、TaskMaster、支持证据时间戳 | 属于用户查询驱动的长时问答，不评价预算约束下的持续自主提醒 |
| 预算记忆与压缩方法 | 摘要、检索、合并、删除以及固定预算下的信息保留 | 多数面向回顾性问答或通用下游效用，不直接优化前瞻提醒与困难沉默决策 |

因此，EgoPM-Bench 不把“主动提醒”“证据删除”或“预算压缩”分别宣称为首次提出，而强调它们在**跨时第一视角前瞻意图生命周期**中的统一任务定义、数据标注和评测协议。

---

## 七、Baselines 与消融实验

### 1. 基础基线

- **Current Observation Only**：不保存持久记忆；
- **Full History**：访问完整历史，作为诊断上界；
- **FIFO / Recency Memory**：按时间保留最近信息；
- **Generic Summary**：使用通用摘要压缩历史；
- **Todo Ledger**：只维护结构化意图及状态账本；
- **Similarity Top-k**：在预算内按当前观察与历史文本相似度选择信息；
- **Budgeted Consolidation**：使用通用合并、保留和删除策略；
- **Oracle Evidence Memory**：优先保留 gold 支持事实，作为证据选择上界。

### 2. CuePM 消融

- 去掉证据监督；
- 去掉状态账本硬约束；
- 去掉完成/取消困难负样本；
- 去掉提醒历史；
- 静态重要性替代决策条件价值；
- 不同证据合并策略；
- 不同 token 预算和不同模型骨干。

### 3. 主要研究问题

- **RQ1**：现有多模态模型能否在无查询流式观察中正确维护并触发前瞻意图？
- **RQ2**：持久记忆预算缩小时，错误主要来自意图遗忘、状态失效还是触发推理？
- **RQ3**：面向未来决策的证据监督是否优于通用摘要、相似度和 recency 策略？
- **RQ4**：生命周期账本能否减少完成、取消、过期后的误提醒和重复提醒？
- **RQ5**：方法提升是否能跨预算、跨意图类型和跨模型骨干保持一致？

---

## 八、数据可行性与风险控制

### 1. 完整生产规模与接受标准

本项目不另设可混入 benchmark 的 P0/P1 或 smoke 数据集。模型/Schema 校准材料不进入正式数据，只用于在大规模调用前证明协议可用。正式规模由“BGE selection → Cue v2 → 候选 Seed → 审计接受 → 配对派生”产生：

- 从全量 Atom 以 BGE-M3 筛选 8,000–12,000 个候选 Atom；
- 由三个百炼账号使用同一 Flash 协议生成约 3,000–5,000 条高质量 Cue；
- 从 Cue v2 生成 900–1,400 个候选 Reminder Seed；
- 每个候选必须具有 1 个真实 trigger atom、至少 2 个真实 lure/distractor atom、完整 terminal/silent 逻辑和同 split 来源；
- 经程序、模型意见和人工终审冻结 480 个基础任务，实际接受数及拒绝原因必须报告；
- 每个基础任务生成 `3 个难度 × 2 个反事实历史 = 6` 条 life-log；
- 480 个基础任务得到 2,880 条 life-log；
- 100–120 小时目标指 `manifest_referenced_hours`，同时必须报告去重后的 `unique_source_hours` 与虚拟跨度 `virtual_span_hours`，不能把重复反事实引用当成新增原始视频时长；
- Decision Instance 数量由每条流的实际事件步数展开后报告，不预先用未经生成的数据虚构确定值。

正式数据必须统计候选 Seed 接受率、各 predicate clause dimension 与场景覆盖、状态变化覆盖、每类困难 silent 数量、短中长 token 负载分布、反事实动作翻转成功率和不同预算下的账本/证据长度。每个阶段还必须按模型、split、参与者、模态和分片生成 `audit/distributions/<stage>/<snapshot_id>/` 分布报告；多账号阶段另按内部 worker 报告漂移，并由 SUCCESS 绑定其 manifest SHA。

### 2. 主要风险及应对

#### 可用 cue 或可解基础任务数量不足

不筛天然完整 Episode，只要求 Source Video Atom 中存在清晰可观察 Cue；意图、生命周期和反事实历史由显式规则构造。若 900–1,400 个候选仍不足 480 个通过审计，应按冻结检索排序扩大 Cue/Seed 候选池，不得降低可观察性与状态机可解性标准。

#### 当前画面直接泄露答案

构建当前场景相同、历史意图或状态不同的 Counterfactual Pair，检验模型是否真正使用历史记忆。

#### 多模态感知与记忆管理混淆

v1 使用“官方事件文本 × 完整历史/预算记忆”两种设置；原始多模态的两种设置作为后续视觉扩展，使用完全相同的 atom ID、Episode 与 gold。

#### 提醒时机具有主观性

标注有效时间窗口而非单一时间点；制定统一冷却和重复提醒政策；报告标注者一致性。

#### 共享事件跨视角泄漏

使用 `world_event_id` 将所有同步视角整体划分，并检查字幕、对话和相邻片段重复。

#### CuePM 退化为普通 todo ledger

将 Todo Ledger 作为强基线，并通过证据监督、困难沉默点、极小预算和语义证据覆盖消融证明 CuePM 的额外价值。如果 CuePM 不能稳定超过账本与通用预算压缩，应将论文定位为 benchmark 加强基线，而不是过度宣称算法创新。

---

## 九、下一步执行顺序

1. 保持已冻结 Source Video Atom 和 split 不变；
2. 实现 Cue/Seed v2 Schema、三账号隔离账本、租约、语义门和分布报告；
3. 在 WSL 专用环境部署固定 BGE-M3，生成并导入 8,000–12,000 个候选 Atom；
4. 三个阿里云账号用同一 `qwen3.7-flash` 协议并行生成 Cue v2；
5. 协调器从隔离 staging 生成约 3,000–5,000 条正式 Cue、manifest、分布报告和 SUCCESS；
6. 冻结 Seed 生产模型，生成 900–1,400 个候选，并检查 trigger、两个 lure、terminal/silent 与 split 条件；
7. 完成机器门、低成本模型意见和全部人工终审，冻结 480 个基础任务与 rule bank；
8. 生成 positive/negative 反事实历史和 short/medium/long 难度版本；
9. 实现 oracle 状态机、Evidence Set 生成器、全量验证器与流式评分器；
10. 输出 2,880 条正式 Life Log，并报告 manifest/unique/virtual 三种时长；
11. 完成无记忆、完整历史、FIFO、摘要和 Todo Ledger 基线；
12. 根据正式数据的记忆长度分布冻结预算档位；
13. 实现 CuePM 的状态账本、证据缓存和在线价值预测器；
14. 在多预算、多意图类型和至少两个模型骨干上完成主实验与消融；
15. 文本版结论成立后，再按 Atom manifest 延迟加载原视频，实施 Visual Extension。

论文能否成立，首先取决于 EgoPM-Bench 是否定义清楚、数据是否可解、划分是否无泄漏、反事实是否真正要求历史、评测是否可复现；CuePM 的创新强度则由其相对 Todo Ledger 和通用预算压缩方法的稳定增益决定。
