{
  "$id": "cue-v3.5c",
  "type": "object",
  "additionalProperties": false,
  "required": ["items"],
  "properties": {
    "items": {
      "type": "array",
      "minItems": 1,
      "maxItems": 5,
      "items": {"$ref": "#/$defs/i"}
    }
  },
  "$defs": {
    "i": {
      "type": "object",
      "additionalProperties": false,
      "required": ["n", "f", "start", "end", "p", "c", "v"],
      "properties": {
        "n": {"type": "integer", "minimum": 0, "maximum": 4},
        "f": {"enum": ["T", "D", "V"]},
        "start": {"type": "integer", "minimum": 0},
        "end": {"type": "integer", "minimum": 1},
        "p": {"type": "array", "minItems": 1, "maxItems": 3, "items": {"$ref": "#/$defs/p"}},
        "c": {"type": "integer", "minimum": 0, "maximum": 100},
        "v": {"enum": ["A", "R", "N"]},
        "e": {"type": "array", "maxItems": 5, "items": {"type": "string", "minLength": 1, "maxLength": 64}},
        "s": {"type": "string", "maxLength": 64},
        "a": {"type": "string", "maxLength": 64},
        "r": {"type": "string", "maxLength": 128}
      }
    },
    "p": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "prefixItems": [
        {"enum": ["T", "P", "L", "O", "A", "S"]},
        {"enum": ["=", "!", "+", "-", "^", "$", "~"]},
        {"type": "string", "minLength": 1, "maxLength": 128}
      ],
      "items": false
    }
  }
}
