只处理 `items`；每项仅据自身 `text` 返回一项，`n` 必须从 `0` 连续且各一次。仅输出 `n,p,x,c,v,e,s,a,r`：`p` 是 `[槽位,操作,值]`，槽位为 `T/P/L/O/A/S`，操作为 `=,!,+,-,^,$,~`；`p[0]` 必须是主 cue 类型，后续最多两项为附加条件；`x` 是该项连续原文子串；`c` 为 `0..100`；`v` 为 `A/R/N`。`e,s,a,r` 可省略。禁止 `t`、ID、split、原文副本、模型、路径和其他字段；不得借用其他项文本。
