# EgoPM 代码清理执行指导（重建最小主线版）

> 定稿日期：2026-09-16
> 本文档取代原 `codex对话记录md`（原始问答记录）。原始版本可通过 Git 历史恢复：
> `git show HEAD:codex对话记录md`（该文件在提交 `a6712a2 codex9.16 简化清理版` 中已被跟踪）。
> 也取代 `代码整理与去治理优化执行方案.md`（84 KB，不再增量维护，见批次 3）。

---

## 0. 使用说明与硬性前提

这份文档是**执行手册**，不是讨论稿。往下读到底、照着批次做，不要再派生新的阶段、门禁、合同或验收体系。

三条红线，任何批次都不得触碰：

| 红线 | 含义 |
| --- | --- |
| 数据事实正确性 | Cue 必须 Source-grounded，evidence span 必须是连续原文，人物角色/否定/时间语气不得被改写 |
| API 安全与费用可控 | Key 只读环境变量、必须显式 `--execute`、有 `--limit`/超时/有限重试/费用上限 |
| 有效数据可恢复 | 动刀前必须先有恢复点（Git tag + 仓库外备份） |

一条必须记住的数据事实：

> `cues/wsl_BGE/candidate_atoms.jsonl` 的 10,000 行**只有 `atom_id`**，不含任何事实文本。
> 禁止直接送入模型。必须先按 `atom_id` 与 `source/source_video_atoms.jsonl` 联结。

本项目的定位：**只为论文 benchmark 做数据处理前置工作**。当前已完成到「WSL 用 BGE 从 370,799 个 Source Atom 中筛出 10,000 个候选」，下一步是用这些候选构造 Cue。Seed 与 Life Log 尚未开始。

---

## 1. 当前真实状态（2026-09-16 实测）

### 1.1 已经有效的成果

- 原始 EgoLife SRT：`raw/EgoLifeCap/`（Transcript 402 + DenseCaption 406，只读）。
- Source 建库完成：`egopm_bench_v1/source/source_video_atoms.jsonl` 共 370,799 行。
- split 完成：`egopm_bench_v1/source/source_split_map.jsonl`。
- BGE 筛选完成：`egopm_bench_v1/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/` 下五个文件，候选 10,000 行，proof 59,465 行。
- Source 阶段脚本 `01`–`04` 已跑通并可复跑。

### 1.2 复杂度与体积（实测）

| 对象 | 实测 |
| --- | --- |
| `scripts/05_extract_cues.py` | 290 KB（约 4,600 行），把 Cue v2 验证、协议验收、旧 V9、realtime API、八波执行、账本、恢复、冻结、分片全混在一起 |
| `scripts/11_validate_all.py` | 162 KB（约 2,600 行），主要验证旧 SUCCESS、哈希、75 分片和 realtime 体系 |
| `scripts/06` / `07` | 25 KB / 32 KB，仍围绕旧 Cue manifest、SUCCESS 和 75 分片设计，无法消费新版 Cue |
| `scripts/08`–`10` / `12` | 11–21 KB，属尚未落地或已废弃的阶段 |
| `cues/realtime/` | 约 37.6 万个文件、约 0.98 GiB（量级取自对话记录，本次遍历超时未能实测，以批次 1.5 的实测结果为准）。**被 .gitignore 排除，删除后不可从 Git 恢复** |
| `cues/formal/` | 75 个 `task_*.jsonl`，旧 V9 Cue 分片（Git 中已跟踪，可恢复） |
| `source/source_video_atoms.jsonl` | 501 MB |
| `source/source_split_map.jsonl` | 101 MB |
| `source/raw_srt_segments.jsonl` | 350 MB（可重建缓存） |
| `source/source_video_atoms_draft.jsonl` | 509 MB（可重建缓存） |
| 未提交改动 | 25 个文件（含 `AGENTS.md`、`codex对话记录md`、`scripts/07`–`12`、多个测试） |

### 1.3 当前实际阻断点（进入 Cue 生产前必须消掉）

1. `AGENTS.md` 仍是完整合同（T0–T4 所有权、SUCCESS 哈希门、工件与目录节制硬门禁），并把 Cue v2 正式布局写成 `cues/v2/formal/` + 正式分片 + 阶段 SUCCESS，与本方案的单 run + 轻量 checkpoint 直接冲突。
2. `config/model_registry.yaml` 的 `cue_v2_extraction.runs_root` 指向不存在的 `../cues/v2/runs`，并带 `run_manifest_required_fields`；`config/paths.yaml` 仍带 `success_markers` 与 `atomic_write.success_marker_required_fields`。
3. 两份旧 Cue v2 Schema 未对齐三种终态：inference 的 `non_accepted.disposition` 含 `unsupported`，artifacts 的 `base.status` 含 `needs_review` / `deferred_unsupported`；且 `tests/test_cue_v2_current.py` 用断言锁死了这些旧值。
4. `05_extract_cues.py` 仍含整套 900/120 验收计划（`build_protocol_acceptance_plan`、`preflight_bge_selection_snapshot`、`partition_cue_v2_candidates`、`_cue_v2_calibration_records`），只被 `tests/test_qwen_contracts.py` 引用。
5. `05` 的 CLI 缺 `--max-requests` / `--max-cost-cny` / `--timeout-seconds` / `--batch-size` / `--resume` / `--finalize` / `--retry-failed`。
6. `06` / `07` / `11` 默认仍消费 `cue_library_manifest` 与 `cue_candidate.schema.json`。

**结论：这套代码应当拆除重建，而不是继续修补。**

---

## 2. 保留 / 删除 总原则

问题从来不是「检查太多」，而是把**四种完全不同的东西**揉成了一套日常开发硬门。必须做这个拆分：

| 类别 | 处置 | 说明 |
| --- | --- | --- |
| 研究事实约束 | **保留** | Source 回查、连续 evidence span、人物角色、否定与时间语气、单 Atom 最多一条 Cue、1–3 个 clause、无法确认则 `no_cue`、constructed 只能从 Seed/Life Log 开始 |
| API 安全与费用上限 | **保留** | Key 只读环境变量、显式 `--execute`、请求上限、超时、有限重试、费用上限 |
| Cue 断点续跑 | **保留（缩为最薄一层）** | 只是一个本地 checkpoint，防止网络中断后重复付费；**不是** ledger / manifest / 治理体系 |
| 发布追溯 | **删除** | manifest、SUCCESS、上游哈希读门、版本相等检查 |
| 多人审批 | **删除** | T0–T4 所有权、CHANGE_REQUEST、handoff、状态板、角色分工 |
| 版本冻结 | **删除** | 合同版本、Schema 版本相等、冻结流程、授权文件 |
| 旧式运行恢复体系 | **删除** | worker、wave、lease、ledger、receipt、progress、协议哈希链、审批短语 |

### 2.1 四类必须保留

1. **不可替代的数据**：原始 EgoLife SRT、`source_video_atoms.jsonl`、`source_split_map.jsonl`、10,000 条 `candidate_atoms.jsonl`（及 BGE 五文件整体备份）、`srt_inventory.csv`。
2. **已验证有效的生产代码**：`01`–`04`，以及与 Source 解析、时间对齐、split 有关的有效测试。
3. **科学设计**：Cue 事实边界（见第 5 节全文）、最终 gold 由确定性状态机产生。
4. **运行安全**：上面表格中的「API 安全」与「最薄 checkpoint」。

### 2.2 可以彻底退出主线

- 根目录与 `coordination/` 下的 CHANGE_REQUEST、状态板、handoff、角色分工、T0–T4 制度。
- contract / config / schema 版本相等检查。
- SUCCESS、manifest、上游哈希作为读取门禁的全部逻辑。
- 旧 V9 Cue 分片、manifest、SUCCESS（`cues/formal/`、`cues/cue_library_manifest.json`、`cues/CUE_LIBRARY_SUCCESS.json`）。
- realtime 的八波、package、账本、receipt、进度、租约、授权文件与恢复状态。
- 旧 Cue prompt、旧 compact Schema、旧 fixture。
- `tests/test_contract_freeze.py` 及旧 realtime / manifest 测试。
- 尚未投入真实生产的 Seed / Life Log 脚本（`06`–`10`）。状态机思想留下，生产外壳不留。
- 旧全仓验证器与统计器（`11`、`12`）。
- `wsl_BGE-M3_filter/` 中重复的治理说明（WSL 端已完整保留筛选实现，Windows 仓库不再承担恢复责任）。

历史恢复依靠 Git tag 与仓库外压缩包，**不要在活动仓库里再建庞大的 `legacy/`**。

---

## 3. 目标结构（一步到位）

### 3.1 目标目录树

```text
egopm_bench_v1/
├─ source/                          # 数据资产，原路径不动（体积大、被 .gitignore 排除）
│  ├─ source_video_atoms.jsonl      # 370,799 行
│  ├─ source_split_map.jsonl
│  ├─ srt_inventory.csv
│  ├─ atom_build_report.json
│  ├─ SOURCE_ATOMS_SUCCESS.json     # 仅 Source 阶段自检，不再作为下游读门
│  ├─ raw_srt_segments.jsonl        # 可重建缓存（批次 6 再处理）
│  └─ source_video_atoms_draft.jsonl# 可重建缓存（批次 6 再处理）
├─ cues/
│  ├─ wsl_BGE/                      # WSL 返回物，原样保存；代码不得当作运行门禁
│  │  ├─ candidate_atoms.jsonl      # 10,000 行，只有 atom_id
│  │  ├─ selection_proof.jsonl      # 59,465 行
│  │  ├─ selection_report.json
│  │  ├─ selection_manifest.json
│  │  ├─ BGE_FILTER_SUCCESS.json
│  │  ├─ WSL_HANDOFF.md
│  │  └─ requirements-bge-m3.lock
│  ├─ inputs/                       # 由 prepare_inputs.py 生成
│  │  └─ selected_atoms.jsonl
│  └─ runs/<run_id>/
│     ├─ cue_results.jsonl
│     └─ run.json
├─ scripts/
│  ├─ source/                       # 原 01–04，原样封存，不再开发
│  │  ├─ 01_inventory_srt.py
│  │  ├─ 02_parse_srt.py
│  │  ├─ 03_align_modal_text.py
│  │  └─ 04_make_source_splits.py
│  └─ cues/                         # 新版 Cue，唯一当前开发主线
│     ├─ prepare_inputs.py
│     ├─ generate_cues.py
│     └─ validate_cues.py
├─ rules/
│  └─ state_machine.py              # 保留：确定性 gold 状态机（见 3.3）
├─ config/
│  ├─ model_registry.yaml           # 精简：只留模型 ID 与基础参数
│  ├─ paths.yaml                    # 精简：只留 01–04 所需路径
│  └─ split_policy.yaml             # 01–04 依赖，保留
├─ prompts/
│  └─ cues/
│     ├─ extraction.md
│     ├─ factual_rules.md
│     ├─ examples.md
│     └─ repair.md                  # 仅在确实需要局部修复时使用
├─ schemas/
│  ├─ source_video_atom.schema.json        # 03/04 依赖，必须保留
│  ├─ source_video_atom_draft.schema.json  # 03/04/测试依赖，必须保留
│  └─ cues/
│     └─ cue.schema.json
└─ tests/
   ├─ source/
   │  └─ test_source_pipeline.py    # 搬移后修正路径
   ├─ cues/
   │  ├─ test_cue_facts.py
   │  └─ test_cue_results.py
   └─ test_state_machine.py         # 保留原样，不搬移
```

**明确不预建**：`scripts/seeds/`、`scripts/lifelogs/`、`tests/seeds/`、`tests/lifelogs/`、`benchmark/`、`logs/`。这些阶段真正开始时再加，不为「预留扩展」提前建目录。

### 3.2 路径映射表（旧 → 新）

| 旧路径 | 新路径 | 方式 |
| --- | --- | --- |
| `egopm_bench_v1/scripts/01_inventory_srt.py` | `egopm_bench_v1/scripts/source/01_inventory_srt.py` | `git mv` + 改 1 行默认路径 |
| `egopm_bench_v1/scripts/02_parse_srt.py` | `egopm_bench_v1/scripts/source/02_parse_srt.py` | 同上 |
| `egopm_bench_v1/scripts/03_align_modal_text.py` | `egopm_bench_v1/scripts/source/03_align_modal_text.py` | 同上 |
| `egopm_bench_v1/scripts/04_make_source_splits.py` | `egopm_bench_v1/scripts/source/04_make_source_splits.py` | 同上 |
| `egopm_bench_v1/tests/test_source_pipeline.py` | `egopm_bench_v1/tests/source/test_source_pipeline.py` | `git mv` + 改 `ROOT`/`SCRIPTS` |
| `egopm_bench_v1/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/*` | `egopm_bench_v1/cues/wsl_BGE/*` | `git mv`（五个文件） |
| `WSL_HANDOFF.md`（仓库根） | `egopm_bench_v1/cues/wsl_BGE/WSL_HANDOFF.md` | `git mv` |
| `requirements-bge-m3.lock`（仓库根） | `egopm_bench_v1/cues/wsl_BGE/requirements-bge-m3.lock` | `git mv` |
| `egopm_bench_v1/source/*` | 不变 | 保持原路径 |

> 注：仓库根的 `WSL_HANDOFF.md` / `requirements-bge-m3.lock` 与 `wsl_BGE-M3_filter/src/` 下的同名文件内容一致（均 5141 / 1669 字节），取任一份即可。

### 3.3 `rules/` 的处置

- `rules/state_machine.py`（8.9 KB）**保留**：确定性单意图状态机，实现「最终 gold 由确定性状态机产生」这条科学要求，不依赖任何旧治理。
- `rules/compiler_io.py`（10.5 KB）**删除**：其文档注释明确写着「受门禁 I/O 工具……验证哈希和 Schema……写入最终标记」，是 SUCCESS/哈希/Schema 门禁的实现本体。
- `rules/__init__.py` **改写**：去掉「阶段 08–10 共享」的表述，只说明本包提供确定性规则实现。
- `tests/test_state_machine.py` 保留原样，不搬移、不改。

---

## 4. 执行批次

所有命令在仓库根目录 `D:\scientific\EgoPM` 下执行（Git Bash 或 WSL 均可）。
`R=egopm_bench_v1` 为下文简写。

> 执行节奏：**一个批次做完、通过验收、再进下一批**。不要跳批，不要在批次之间顺手改别的东西。
> 批次之间发生的新问题，直接改代码或改本文档的「附录 C 临时清单」，不要再开新的阶段或合同。

---

### 批次 1：保护当前成果（唯一不可跳过的一步）

**目标**：任何删除动作之前，先保证现在能恢复。

```bash
cd /d/scientific/EgoPM
R=egopm_bench_v1

# 1.1 记录现状（留档，不进仓库）
git status --short
git log --oneline -5

# 1.2 把 25 个未提交改动落成一个提交，避免后面搬迁时丢失
git add -A
git commit -m "cleanup: 清理前工作区快照"

# 1.3 打恢复点 tag
git tag cleanup-pre-20260916-01

# 1.4 仓库外备份（不可从 Git 恢复的部分）
mkdir -p ../_egopm_backup_20260916/{source,bge,realtime_manifest}
cp $R/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/*.jsonl ../_egopm_backup_20260916/bge/
cp $R/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/*.json     ../_egopm_backup_20260916/bge/
cp $R/source/source_video_atoms.jsonl        ../_egopm_backup_20260916/source/
cp $R/source/source_split_map.jsonl          ../_egopm_backup_20260916/source/
cp $R/source/raw_srt_segments.jsonl          ../_egopm_backup_20260916/source/
cp $R/source/source_video_atoms_draft.jsonl  ../_egopm_backup_20260916/source/
cp $R/source/srt_inventory.csv               ../_egopm_backup_20260916/source/

# 1.5 删除 realtime 之前先记录体积与文件数（它不进 Git，删了就没有了）
du -sh $R/cues/realtime > ../_egopm_backup_20260916/realtime_manifest/size.txt
find $R/cues/realtime -type f | wc -l >> ../_egopm_backup_20260916/realtime_manifest/size.txt
ls $R/cues/realtime/runs > ../_egopm_backup_20260916/realtime_manifest/run_ids.txt
```

**1.6 校验 10,000 个候选都能与 Source 联结**（这是后面 Cue 阶段的前提）：

```bash
python - <<'PY'
import json
R = "egopm_bench_v1"
cand = [json.loads(l)["atom_id"] for l in open(f"{R}/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02/candidate_atoms.jsonl", encoding="utf-8")]
print("候选行数:", len(cand), "唯一:", len(set(cand)))
want = set(cand)
hit = set()
for l in open(f"{R}/source/source_video_atoms.jsonl", encoding="utf-8"):
    a = json.loads(l)
    if a["atom_id"] in want:
        hit.add(a["atom_id"])
print("在 Source 中命中:", len(hit), "缺失:", len(want - hit))
print("缺失样例:", list(want - hit)[:5])
PY
```

**验收**：`git tag` 能看到 `cleanup-pre-20260916-01`；仓库外备份目录大小与源一致；上一步输出「候选行数: 10000 唯一: 10000」且「缺失: 0」。

**回滚**：`git reset --hard cleanup-pre-20260916-01`。

**⚠️ 执行前需你确认**：`cues/realtime/`（约 37.6 万文件 / 0.98 GiB，量级待批次 1.5 实测）被 `.gitignore` 排除，删除后**无法从 Git 恢复**。确认它确实无研究价值后，才在批次 3 执行 `rm -rf`。

---

### 批次 2：建立新骨架并搬迁数据资产

**目标**：先把「要保留的」全部搬到新位置，再考虑删除。这样即使中途出错，也只是多了一堆旧文件。

```bash
cd /d/scientific/EgoPM
R=egopm_bench_v1

# 2.1 建立新骨架（不建 seeds/lifelogs/benchmark/logs）
mkdir -p $R/scripts/source $R/scripts/cues
mkdir -p $R/tests/source $R/tests/cues
mkdir -p $R/prompts/cues $R/schemas/cues
mkdir -p $R/cues/wsl_BGE $R/cues/inputs

# 2.2 Source 脚本搬迁
git mv $R/scripts/01_inventory_srt.py   $R/scripts/source/01_inventory_srt.py
git mv $R/scripts/02_parse_srt.py       $R/scripts/source/02_parse_srt.py
git mv $R/scripts/03_align_modal_text.py $R/scripts/source/03_align_modal_text.py
git mv $R/scripts/04_make_source_splits.py $R/scripts/source/04_make_source_splits.py

# 2.3 Source 测试搬迁
git mv $R/tests/test_source_pipeline.py $R/tests/source/test_source_pipeline.py

# 2.4 WSL 返回物搬入 cues/wsl_BGE（原样，不改内容）
B=$R/cues/v2/source_selection/bge_m3_source_select_v3_1_20260914_02
for f in candidate_atoms.jsonl selection_proof.jsonl selection_report.json selection_manifest.json BGE_FILTER_SUCCESS.json; do
  git mv "$B/$f" "$R/cues/wsl_BGE/$f"
done
git mv WSL_HANDOFF.md "$R/cues/wsl_BGE/WSL_HANDOFF.md"
git mv requirements-bge-m3.lock "$R/cues/wsl_BGE/requirements-bge-m3.lock"
```

**2.5 搬家后必须做的两处路径修正**（这是「原样搬迁」唯一允许的改动）：

- `scripts/source/01_inventory_srt.py`、`02_parse_srt.py`、`03_align_modal_text.py`、`04_make_source_splits.py`
  各有一处 `--config` 默认值，从 `parents[1]` 改成 `parents[2]`：

  ```python
  # 改前
  default=Path(__file__).resolve().parents[1] / "config" / "paths.yaml",
  # 改后
  default=Path(__file__).resolve().parents[2] / "config" / "paths.yaml",
  ```

  （等价做法：不改代码，每次显式传 `--config egopm_bench_v1/config/paths.yaml`。二者选一，不要两边都做。）

- `tests/source/test_source_pipeline.py`：

  ```python
  # 改前
  ROOT = Path(__file__).resolve().parents[1]
  SCRIPTS = ROOT / "scripts"
  # 改后
  ROOT = Path(__file__).resolve().parents[2]
  SCRIPTS = ROOT / "scripts" / "source"
  ```

**验收**：

```bash
python $R/scripts/source/01_inventory_srt.py --help          # 正常输出帮助
python -m pytest egopm_bench_v1/tests/source -q              # 3 passed
ls $R/cues/wsl_BGE                                           # 7 个文件齐全
```

**回滚**：`git reset --hard cleanup-pre-20260916-01 && git clean -fd`。

> 说明：`01`–`04` 目前会写 `SOURCE_ATOMS_SUCCESS.json` 与哈希。**封存期内保留不动**——它是 Source 阶段自己的自检，只是不再被当作下游读门。同样，`test_source_pipeline.py` 中现有的 SUCCESS / 哈希断言也保留，因为 Source 已封存；新版 Cue 与未来阶段**不得**再读它们。

---

### 批次 3：删除旧代码与治理

**目标**：把整个旧治理体系与失效阶段一次移出主线。此时历史都能从 Git / 仓库外备份恢复。

```bash
cd /d/scientific/EgoPM
R=egopm_bench_v1

# 3.1 删除失效阶段脚本（05–12 全部）
git rm $R/scripts/05_extract_cues.py $R/scripts/06_retrieve_trigger_lures.py \
       $R/scripts/07_generate_seed_candidates.py $R/scripts/08_freeze_seed_audit.py \
       $R/scripts/09_build_lifelog_families.py $R/scripts/10_run_oracle.py \
       $R/scripts/11_validate_all.py $R/scripts/12_build_statistics.py

# 3.2 删除治理测试与旧 fixture
git rm $R/tests/test_contract_freeze.py $R/tests/test_cue_v2_current.py \
       $R/tests/test_qwen_contracts.py $R/tests/test_validators.py
git rm -r $R/tests/fixtures

# 3.3 删除门禁 I/O 与旧规则外壳
git rm $R/rules/compiler_io.py

# 3.4 删除协作治理目录
git rm -r $R/coordination

# 3.5 删除旧 V9 Cue 分片与治理标记
git rm -r $R/cues/formal
git rm $R/cues/cue_library_manifest.json $R/cues/CUE_LIBRARY_SUCCESS.json

# 3.6 删除旧 prompt
git rm $R/prompts/cue_extractor_v1.md $R/prompts/cue_extractor_v2.md \
       $R/prompts/cue_extractor_v3_compact.md \
       $R/prompts/seed_auditor_v1.md $R/prompts/seed_generator_v1.md

# 3.7 删除旧 Schema（保留 source_video_atom / source_video_atom_draft 两个）
git rm $R/schemas/cue_candidate.schema.json $R/schemas/cue_inference_batch_v1.schema.json \
       $R/schemas/cue_inference_batch_compact_v1.schema.json \
       $R/schemas/cue_v2_inference.schema.json $R/schemas/cue_v2_artifacts.schema.json \
       $R/schemas/trigger_lure_set.schema.json $R/schemas/decision_instance.schema.json \
       $R/schemas/lifelog.schema.json $R/schemas/reminder_seed.schema.json

# 3.8 删除治理配置
git rm $R/config/benchmark_protocol.yaml $R/config/CUE_EXECUTION_AUTHORIZATION.example.json

# 3.9 删除 WSL 端重复说明（WSL 已完整保留筛选实现）
git rm -r wsl_BGE-M3_filter

# 3.10 未跟踪 / 被忽略的目录
rm -rf $R/audit $R/benchmark $R/lifelogs $R/seeds $R/logs
rm -f  $R/cues/CUE_EXECUTION_AUTHORIZATION.json
rm -rf $R/cues/realtime          # ⚠️ 见批次 1 的确认项
rm -rf $R/cues/v2                # 五文件已搬走后剩下的空目录
```

**3.11 精简 `config/model_registry.yaml`**

- 删除 `cue_v2_extraction.runs_root`（指向不存在的 `../cues/v2/runs`）。
- 删除 `run_manifest_required_fields`。
- 删除 `worker_resources`、`ledger_path`、`realtime_*` 等治理键。
- 只保留：模型 ID（Cue 抽取用 `qwen3.7-flash`）、接口基址、基础采样参数。

**3.12 精简 `config/paths.yaml`**

- 删除 `success_markers` 整段。
- 删除 `atomic_write.success_marker_required_fields`。
- 删除 `cue_v2` 整段（Cue 阶段路径由 `scripts/cues/` 内部常量与 CLI 决定）。
- 删除 `benchmark_protocol` / `contract_version` 之类引用。
- 只保留 `01`–`04` 实际读取的键：`transcript_root` / `dense_caption_root` / `source_build`（含 `schema_dir`、各输出路径）、指向 `split_policy.yaml` 的目录等。

**3.13 精简 `pyproject.toml`**

```toml
# 删除 markers 中的 legacy_v36 与 cue_v2
# 删除 addopts 里的 "-m 'not legacy_v36'"，只留 "-ra"
[tool.pytest.ini_options]
testpaths = ["egopm_bench_v1/tests"]
addopts = "-ra"
python_files = ["test_*.py"]

# 删除整个 [tool.egopm] 段（contract_version / full_validation_command 指向已删除的 11_validate_all.py）
```

**3.14 处置仓库根的非代码文档**

按你「第一轮先暂时保留非代码 md」的要求，本批次**不删**，只在附录 A 标注去向。但 `代码整理与去治理优化执行方案.md`（84 KB）已经完成使命且你已决定不再维护它，建议直接搬到 `_archive/`：

```bash
mkdir -p _archive
git mv 代码整理与去治理优化执行方案.md _archive/代码整理与去治理优化执行方案.md
```

**验收**：

```bash
python -m pytest -q                      # 全绿；现基线 13 passed，减去被删的 test_cue_v2_current(2) → 11 passed
git status --short                       # 无残留 D 状态
grep -rn "SUCCESS\|manifest\|sha256\|冻结\|门禁\|T0\|CHANGE_REQUEST" \
     egopm_bench_v1/scripts/cues egopm_bench_v1/config
                                         # 无匹配（scripts/source 与 wsl_BGE 允许保留，见批次 2 说明）
ls egopm_bench_v1                       # 只剩 source cues scripts rules config prompts schemas tests
```

**回滚**：`git reset --hard cleanup-pre-20260916-01 && git clean -fd`，再从 `../_egopm_backup_20260916/` 恢复数据文件。

---

### 批次 4：重写规则与极简清单

**目标**：把「指导日常开发」的唯一文档压到一页以内。

**4.1 重写 `AGENTS.md`**，只保留四节，总长不超过一页：

1. 协作：单人或双人协作，可以直接改代码；不再有 T0–T4、角色分工与合并审批。
2. 安全：API Key 只读环境变量；如需调用模型必须显式 `--execute`；必须有 `--limit`、超时、有限重试与费用上限；不把原始模型响应提交 Git。
3. 中文文档：Markdown 正文与 Python 模块说明/关键注释用中文；字段名、命令、路径、模型 ID 等专名除外。
4. 数据事实边界：直接引用本文档第 5 节的十条，不再复述。

明确删除：T0–T4 所有权、CR 流程、冻结、哈希、SUCCESS、目录预算、合同版本、工件节制硬门禁。

**4.2 建立最小保留清单**（一两页，只列路径与去留，不写几十页理由）。直接用本文档附录 A，执行时导出为独立文件即可。

**4.3 更新 `README.md`**：写清当前进度（已完成 Source + BGE 筛选，下一步 Cue）、如何跑 `scripts/source/`、如何跑 `scripts/cues/`、数据文件都在哪里、哪些文件不进 Git。

**验收**：`AGENTS.md` 长度 ≤ 1 页；`grep -i "T0\|T1\|T2\|T3\|T4\|冻结\|门禁\|合同" AGENTS.md` 无匹配。

**回滚**：`git checkout cleanup-pre-20260916-01 -- AGENTS.md`。

---

### 批次 5：实现并验证新版 Cue

**目标**：得到唯一一个能跑完 10,000 条的正式 Cue 生产器。详细规格见第 5、6 节。

**5.1 摘取少量可复用函数，不要裁剪旧文件。**
旧 `05_extract_cues.py` 已在批次 3 删除，需要时从 Git 取回参考（`git show cleanup-pre-20260916-01:egopm_bench_v1/scripts/05_extract_cues.py`）。只允许**人工重写**以下通用能力，不 import 旧模块、不保留 fallback：

- API 请求与超时处理；
- 结构化响应解析；
- exact span 检查；
- JSON Schema 校验；
- 原子文件替换；
- token / 费用统计。

**5.2 写三个文件**（见 6.1），总量控制在数百行级别。若 `prepare_inputs.py` 足够短，可与 `generate_cues.py` 合并。

**5.3 写 prompt 集合**（见 6.4）。

**5.4 定稿 `schemas/cues/cue.schema.json`**（见 6.5）。

**5.5 先 fixture，再 pilot，最后全量。**

1. 用少量手写 fixture 跑通 `validate_cues.py`。
2. 用 50–100 个覆盖典型陷阱的 Atom 做 pilot：人物角色、否定、地点、状态、时间语气。
3. pilot 通过后，**同一个 run_id 直接续跑到 10,000**。首批 N 条就是正式生产的一部分——同样的模型/prompt/Schema/参数/目录/checkpoint/run_id，不叫 smoke，不另建独立目录或独立测试。

**验收**：见第 8 节完成定义。

**回滚**：Cue 阶段没有不可恢复动作；删掉 `cues/runs/<run_id>/` 重跑即可（注意会产生新的 API 费用）。

---

### 批次 6（暂缓，最后再做）：清理可重建大文件

确认新版 Cue 全量跑通、且不再需要回溯 draft 之后，再处理：

- `source/raw_srt_segments.jsonl`（350 MB，可由 `02` 重建）
- `source/source_video_atoms_draft.jsonl`（509 MB，可由 `03` 重建）
- 各类缓存与构建产物：`egopm_bench_v1/**/__pycache__/`、`.pytest_cache/`、`egopm_bench.egg-info/`

```bash
cd /d/scientific/EgoPM
find . -name "__pycache__" -type d -not -path "./.venv/*" | xargs rm -rf
rm -rf .pytest_cache egopm_bench.egg-info
```

**验收标准**：能只靠 `raw/EgoLifeCap/` + `scripts/source/01`–`04` 完整重跑出 `source_video_atoms.jsonl`，才算「可重建」成立。

---

## 5. 新版 Cue 事实边界（十条，不可删）

1. Cue 只能来自同一个 Atom 的 `transcript` 或 `dense_caption`。
2. evidence span 必须是连续原文。
3. BGE 查询族不等于 Cue dimension。
4. 人物「被提及」「说话」「实际在场」必须区分。
5. 不跨 Atom 消解代词。
6. 否定、假设、将来、不确定的话语不能改写成已发生的事实。
7. 一个 Atom 最多一条 Cue；一个 predicate 使用最少且充分的 1–3 个 clause。
8. 无法确认时输出 `no_cue`，不能猜。
9. constructed 内容只能从 Seed / Life Log 开始。
10. 最终 gold 由确定性状态机（`rules/state_machine.py`）产生，禁止大模型写。

---

## 6. 新版 Cue 实现规格

### 6.1 三个文件

```text
scripts/cues/
├─ prepare_inputs.py   # 用 10,000 个 atom_id 联结 Source，生成可审查输入
├─ generate_cues.py    # API 调用、费用限制、轻量 checkpoint、结果写入
└─ validate_cues.py    # span / clause / 字段 / 否定 / 完整性检查
```

不要重新形成一个几千行的总入口。

### 6.2 `prepare_inputs.py`

读 `cues/wsl_BGE/candidate_atoms.jsonl` 的 10,000 个 `atom_id`，按 `atom_id` 联结 `source/source_video_atoms.jsonl`，输出：

```text
cues/inputs/selected_atoms.jsonl
```

每行只含：

```json
{
  "atom_id": "...",
  "transcript": "...",
  "dense_caption": "...",
  "source_group_id": "..."
}
```

**验收**：恰好 10,000 行；`atom_id` 唯一；`transcript` 与 `dense_caption` 至少一个非空。（这是人工可审查的中间产物，也是唯一允许送进模型的输入。）

### 6.3 `generate_cues.py`

四段结构，职责清晰：

1. 读取 `selected_atoms.jsonl`。
2. 调用模型，得到 `accepted` 或 `no_cue`。
3. 做确定性验证（见 6.6 第一、二层）。
4. 写终态并支持恢复。

CLI 必须包含：`--execute`、`--limit`、`--max-requests`、`--max-cost-cny`、`--timeout-seconds`、`--batch-size`、`--resume`、`--retry-failed`、`--finalize`。缺少 `--execute` 时只做只读预检，不发请求。

**恢复只保留最薄一层**：一个本地 checkpoint，记录已完成的 `atom_id`，用于断点续跑、避免重复付费。**不得**引入 worker、wave、lease、ledger、receipt、SUCCESS、协议哈希链或审批短语。

### 6.4 `prompts/cues/`

```text
prompts/cues/
├─ extraction.md       # 核心任务、输出结构、no_cue 原则
├─ factual_rules.md    # 人物、否定、时间、证据和指代边界
├─ examples.md         # 少量正反例
└─ repair.md           # 仅在确实需要局部修复时使用
```

拆分依据是**职责**，不是机械控制行数。每份要能完整表达一个主题，避免几行的小碎片，也避免几十 KB 的新巨型 prompt。文件数量可以继续增长。

### 6.5 `schemas/cues/cue.schema.json` 与结果文件

结果目录：

```text
cues/runs/<run_id>/
├─ cue_results.jsonl
└─ run.json
```

`cue_results.jsonl` 覆盖**全部** 10,000 个 Atom 的终态：

```json
{"atom_id":"...","status":"accepted","cue":{...}}
{"atom_id":"...","status":"no_cue","reason":"UNRESOLVED_REFERENT"}
{"atom_id":"...","status":"failed","reason":"API_TIMEOUT"}
```

只有三种终态：`accepted` / `no_cue` / `failed`。不再有 `unsupported`、`needs_review`、`deferred_unsupported`、`excluded_ambiguous` 之类的中间态，也不再另外创建 accepted、excluded、coverage、ledger、manifest、SUCCESS 多套文件。下游只需筛 `status == "accepted"`。

`run.json` 只记录：模型 ID、生成时间、输入路径、输入条数、accepted / no_cue / failed 数量、费用汇总。**不带哈希，不作为读取门禁，不需要版本号。**

`validate_cues.py` 的完整性检查只需四条：

1. 10,000 个输入 ID 是否各出现一次；
2. 是否有重复 ID；
3. 是否还有 `failed`；
4. accepted 的 Cue 是否通过事实验证。

### 6.6 三层事实验证的边界（必须承认的能力上限）

- **第一层（程序可完全解决）**：evidence 字段合法、span 是连续原文、clause 数 1–3、dimension / operator 合法、禁止空值与泛指词、禁止明显未解析代词。
- **第二层（程序可大部分解决）**：否定和时间语气没有被反转。
- **第三层（程序无法完全解决）**：「完整语义是否被 span 支持」。

第三层的合理做法是：**prompt 严格约束 + 对 pilot 与正式结果做分层人工抽查 + 争议项直接 `no_cue`**。不要为此再建复杂的多模型审计流水线。

发现问题时直接改 prompt、Schema 和代码，然后重跑受影响部分——**不再开版本合同**。

---

## 7. Seed / Life Log 暂停条款

- 当前**不实现**任何 Seed / Life Log 代码。
- `06`–`10` 已删除（注意：这条覆盖了早期「重写 06/07」的想法，以后者为准）。
- 只保留一份简短的 `DESIGN.md`，写清下列设计即可：
  - Cue 作为 trigger；
  - lure 必须同 split；
  - 至少一个同 `source_group_id`；
  - 至少一个跨 `source_group_id`；
  - 每个 lure 明确指出未满足的 predicate clause；
  - 六路 Life Log 的数据结构；
  - 状态机与 remind / silent 判定；
  - gold 由确定性状态机产生。
- 将来真正开始 Seed 阶段时，在 `scripts/seeds/` 从零实现，并直接读取新版 `cue_results.jsonl`。

---

## 8. 完成定义（Definition of Done）

代码清理部分：

1. `egopm_bench_v1/` 下只剩 `source/`、`cues/`、`scripts/`、`rules/`、`config/`、`prompts/`、`schemas/`、`tests/`。
2. 仓库内不存在 `coordination/`、CHANGE_REQUEST、handoff、T0–T4、合同版本、冻结流程、SUCCESS 读门、manifest 读门、哈希读门、worker/wave/lease/ledger/receipt。
3. `scripts/cues/` 与 `config/` 里 grep 不到 `SUCCESS`、`manifest`、`sha256`、`冻结`、`门禁`（`scripts/source/` 与 `cues/wsl_BGE/` 允许保留，因为 Source 已封存、WSL 五文件是原始资料）。
4. `python -m pytest -q` 全绿，收集数在 15 条以内。
5. `AGENTS.md` ≤ 1 页，且不含治理词汇。
6. 一次 `git reset --hard cleanup-pre-20260916-01` 能回到清理前状态。

Cue 阶段部分：

7. `cues/inputs/selected_atoms.jsonl` 恰好 10,000 行，`atom_id` 唯一，且每个都能在 Source 中命中。
8. `cues/runs/<run_id>/cue_results.jsonl` 覆盖全部 10,000 个 Atom，无重复、无遗漏。
9. 不存在未处理的 `failed`（或 `failed` 已单独重跑并给出终态）。
10. 所有 `accepted` 的 Cue 通过 `validate_cues.py` 的第一、二层检查，并已完成 pilot + 正式结果的分层人工抽查。
11. 中途中断后 `--resume` 能续跑且不重复请求已完成的 Atom。

---

## 9. 只在执行时才生成的信息（不要写进方案）

以下内容每次执行临时生成，不要固化进任何文档，否则每次变化都会诱发新一轮文档维护：

- 文件级大小、行数；
- 引用数量、被引用次数；
- Git 状态、未提交文件清单；
- 测试收集数与通过数；
- 目录体积。

需要时临时跑命令即可：

```bash
find egopm_bench_v1 -name "*.py" | xargs wc -l | tail -1
git status --short | wc -l
python -m pytest -q 2>&1 | tail -3
```

---

## 附录 A：路径去留总表

| 路径 | 去留 | 批次 |
| --- | --- | --- |
| `raw/EgoLifeCap/` | 保留（只读原始数据） | — |
| `egopm_bench_v1/source/source_video_atoms.jsonl` | 保留 | — |
| `egopm_bench_v1/source/source_split_map.jsonl` | 保留 | — |
| `egopm_bench_v1/source/srt_inventory.csv` | 保留 | — |
| `egopm_bench_v1/source/atom_build_report.json` | 保留 | — |
| `egopm_bench_v1/source/SOURCE_ATOMS_SUCCESS.json` | 保留（仅自检，不作读门） | — |
| `egopm_bench_v1/source/raw_srt_segments.jsonl` | 暂留，批次 6 再删 | 6 |
| `egopm_bench_v1/source/source_video_atoms_draft.jsonl` | 暂留，批次 6 再删 | 6 |
| `.../bge_m3_source_select_v3_1_20260914_02/*`（5 文件） | 搬入 `cues/wsl_BGE/` | 2 |
| `WSL_HANDOFF.md`（根） | 搬入 `cues/wsl_BGE/` | 2 |
| `requirements-bge-m3.lock`（根） | 搬入 `cues/wsl_BGE/` | 2 |
| `scripts/01`–`04` | 搬入 `scripts/source/`，封存 | 2 |
| `scripts/05`–`12` | **删除** | 3 |
| `scripts/__pycache__/` | 删除 | 3 |
| `tests/test_source_pipeline.py` | 搬入 `tests/source/` | 2 |
| `tests/test_state_machine.py` | 保留原样 | — |
| `tests/test_contract_freeze.py` | **删除** | 3 |
| `tests/test_cue_v2_current.py` | **删除** | 3 |
| `tests/test_qwen_contracts.py` | **删除** | 3 |
| `tests/test_validators.py` | **删除** | 3 |
| `tests/fixtures/` | **删除** | 3 |
| `rules/state_machine.py` | 保留 | — |
| `rules/compiler_io.py` | **删除** | 3 |
| `rules/__init__.py` | 改写 | 3 |
| `coordination/`（含 CHANGE_REQUESTS、STATUS、handoffs） | **删除** | 3 |
| `cues/formal/`（75 个 task_*.jsonl） | **删除** | 3 |
| `cues/cue_library_manifest.json` | **删除** | 3 |
| `cues/CUE_LIBRARY_SUCCESS.json` | **删除** | 3 |
| `cues/CUE_EXECUTION_AUTHORIZATION.json` | **删除** | 3 |
| `cues/realtime/` | **删除**（不可恢复，需确认） | 3 |
| `cues/v2/`（搬空后的空目录） | 删除 | 3 |
| `prompts/cue_extractor_v1.md` / `v2` / `v3_compact` | **删除** | 3 |
| `prompts/seed_auditor_v1.md` / `seed_generator_v1.md` | **删除** | 3 |
| `schemas/source_video_atom.schema.json` | 保留（03/04 依赖） | — |
| `schemas/source_video_atom_draft.schema.json` | 保留（03/04/测试依赖） | — |
| `schemas/cue_*.json`（4 个） | **删除** | 3 |
| `schemas/trigger_lure_set.schema.json` | **删除** | 3 |
| `schemas/decision_instance.schema.json` | **删除** | 3 |
| `schemas/lifelog.schema.json` | **删除** | 3 |
| `schemas/reminder_seed.schema.json` | **删除** | 3 |
| `config/model_registry.yaml` | 保留并精简（批次 3.11） | 3 |
| `config/paths.yaml` | 保留并精简（批次 3.12） | 3 |
| `config/split_policy.yaml` | 保留（01–04 依赖） | — |
| `config/benchmark_protocol.yaml` | **删除** | 3 |
| `config/CUE_EXECUTION_AUTHORIZATION.example.json` | **删除** | 3 |
| `audit/` | 删除 | 3 |
| `benchmark/`（空） | 删除 | 3 |
| `lifelogs/`（空） | 删除 | 3 |
| `seeds/`（空） | 删除 | 3 |
| `logs/`（空） | 删除 | 3 |
| `wsl_BGE-M3_filter/` | **删除**（WSL 端已保留实现） | 3 |
| `AGENTS.md` | 重写为极简版 | 4 |
| `pyproject.toml` | 精简（批次 3.13） | 3 |
| `README.md` | 更新 | 4 |
| `论文选题.md` | 第一轮保留 | — |
| `EgoPM_Bench_v1_完整生产流水线统筹指南.md` | 第一轮保留，第二轮再定 | 后续 |
| `CHANGE_REQUESTS.md`（根） | 第一轮保留，第二轮再定 | 后续 |
| `代码整理与去治理优化执行方案.md` | 搬入 `_archive/`，不再维护 | 3.14 |
| `.gitignore` | 精简：删除 `cues/realtime/`、`cues/batch/` 等失效条目 | 3 |
| `.venv/` | 保留（运行环境，已被忽略） | — |
| `.codex/` | 保留（工具配置，已被忽略） | — |
| `.pytest_cache/` | 删除（缓存） | 6 |
| `egopm_bench.egg-info/` | 删除（构建产物；若仍以 `pip install -e .` 方式使用则保留） | 6 |

---

## 附录 B：决策记录（最终结论，覆盖过程中的反复）

| 议题 | 最终结论 | 说明 |
| --- | --- | --- |
| 05_extract_cues.py | 不裁剪，**拆除重建** | 内部耦合太重，手术会把历史假设带回来 |
| 11_validate_all.py | 整文件删除，不改造 | 新版只保留各阶段旁的小型验证 |
| 06 / 07 是否重写 | **不重写，直接删除** | 与「Seed/Life Log 暂不实现」的最终决定一致 |
| 06–10 | 全部删除 | Seed/Life Log 只留设计文档 |
| 08 / 09 / 10 / 12 | 删除 | 未落地或已废弃 |
| 旧 V9 / realtime | 直接进入清理批次 | 不再等新版 Cue 全量完成后才清理 |
| WSL 边界 | Windows 仓库不承担筛选程序恢复责任 | WSL 端已完整保留实现 |
| WSL 返回物 | 在 Cue 阶段内保留 | 放入 `cues/wsl_BGE/`，原样保存 |
| 五文件中的 `selection_manifest.json` / `BGE_FILTER_SUCCESS.json` | 保留为原始资料 | 代码**不得**当作运行门禁 |
| Source 阶段 | 原样搬迁封存，不重写 | 只改 `--config` 默认路径深度 |
| 测试 | 按阶段结构组织，只做少量关键核验 | 不再建覆盖旧治理体系的大型套件 |
| Cue 结果文件 | 单一终态文件 | 三种终态，不再拆多套文件 |
| Cue 代码文件数 | 两到三个职责清楚的文件 | 不重建几千行总入口 |
| prompt | 按职责拆成小型集合 | 依据职责，不机械控制行数 |
| 运行恢复 | 只留防重复付费的轻量 checkpoint | 取消 worker/wave/lease/ledger/receipt/SUCCESS/哈希链 |
| 发布追溯、多人审批、版本冻结、哈希读门 | 全部删除 | 与研究事实约束明确区分 |
| 历史恢复方式 | Git tag + 仓库外压缩包 | 不在仓库内建 `legacy/` |

---

## 附录 C：临时清单（执行中发现问题就写在这里）

> 只在执行过程中追加，不设格式要求。执行结束后可清空。
> 若问题涉及本文档本身的错误（路径写错、命令失效），**直接改本文档对应段落**，不要新增一节来「补充说明」。

| 日期 | 批次 | 问题 | 处理 |
| --- | --- | --- | --- |
|  |  |  |  |

---

## 附录 D：待你确认的四个事项

1. **`cues/realtime/` 删除**：量级约 37.6 万文件 / 0.98 GiB（待批次 1.5 实测），被 `.gitignore` 排除，删除后无法从 Git 恢复。确认无研究价值后才执行批次 3.10。
2. **`rules/state_machine.py` 保留**：建议保留（确定性 gold 的唯一已实现资产，8 条测试通过，不依赖旧治理）。若你希望暂停阶段完全不留代码，则一并删除，并在 `DESIGN.md` 中只保留设计描述。
3. **非代码 md 文档**：按你的要求第一轮保留 `论文选题.md`、`EgoPM_Bench_v1_完整生产流水线统筹指南.md`、`CHANGE_REQUESTS.md`，第二轮再决定归档或删除。
4. **`代码整理与去治理优化执行方案.md`**：建议搬入 `_archive/` 而非直接删除，避免误删其中仍有价值的流水线设计描述。

---

## 一句话总结

这次不是「重构旧系统」，而是**保留数据资产，重建最小当前主线**。
可以删掉绝大多数治理、历史代码和未落地阶段；唯一不能一起删的，是事实性约束、API 安全、以及恢复当前有效数据的能力。
