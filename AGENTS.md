# EgoPM-Bench v1 协作规则

本文件是唯一的活动规则。此前的多角色协作与发布治理规则已全部作废，不再有效。

## 1. 协作方式

- 单人/双人直接协作，可以直接修改任何代码与配置。
- 没有角色分工、没有合并审批、没有变更请求流程。
- 一个改动一个提交；回滚用 `git revert`，不要用 `reset --hard`，不要用 `git clean -fd`。

## 2. 安全与费用

- API Key 只从环境变量读取（如 `DASHSCOPE_API_KEY`）。
- 严禁提交密钥、`.env`、原始语料或媒体文件、以及模型原始响应。
- 任何调用模型的命令必须显式传 `--execute`，否则只做只读预检，不发请求。
- 必须实际设置请求上限 `--limit`、超时、有限重试次数与费用上限。
- 生成的 JSONL 不得手改，只能由程序重新生成。

## 3. 中文文档

- 全部 Markdown 正文用中文。
- 每个 Python 文件的首个有效内容必须是中文模块说明，写清：负责什么、输入是什么、输出是什么。
- 关键判断、时间对齐、过滤、状态转移和异常处理必须有中文注释，解释「为什么这样做」，不要复述代码字面。
- 例外：代码、命令、路径、URL、字段名、模型 ID、版本号等专名保持原文，并置于代码格式或代码块中。

## 4. 数据事实边界

十条事实边界规则见仓库根目录 `codex对话记录md` 的第 5 节。它们构成本项目唯一的科学正确性清单，任何 Cue / Seed / Life Log 相关工作都必须遵守。此处不再复述，以免出现两处规则漂移。
